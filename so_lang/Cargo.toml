[package]
name = "so-lang"
version.workspace = true
edition.workspace = true
description = "The constrained specification language: parser, AST, and derived semantic interpretations."

[lib]
name = "so_lang"
path = "src/lib.rs"

[dependencies]
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true
