判定: NG

型付きU0構成の機械検証コアとしては及第点だが、原著論文としては新規性・適用範囲が不十分。

**必須修正:**

1. **新規性の明確化** (`paper/manuscript/uadf_u0_spec_proof.md:4.1`)  
   依存型環境での型検証上の障害を説明したが、これは「実装技術」であり理論的新規性ではない。抽象解釈の随伴接続との関係を明示せず「部分性を一次表現」と主張するのみでは、既存枠組みへの埋め込み可能性を排除できない。**修正案**: 定理4.1として、標準的Galois接続への埋め込みが追加条件なしでは成立しないことの形式的証明を追加し、`Option`型による部分性表現が本質的であることを示す。

2. **外的妥当性の実証** (`paper/case-study/`)  
   合成データ220,000例での一致検証は再現性確認に留まり、実プロジェクト由来アーティファクトでの評価が皆無。パスワード長制約のみでは適用範囲が狭すぎる。**修正案**: OpenSSL/PostgreSQL等のOSS要求仕様から最低3種類の異型制約（数値範囲・列挙・排他）を抽出し、矛盾検出率・漏れ検出率を実測。Lean証明との整合性を報告。

3. **層間合成則の一般化** (`paper/lean/UadfU0/InterLayer/Composition.lean:35-66`)  
   定理9は`proj_j = bind(proj_i, g)`という特殊形態に限定。一般の抽出器パイプライン（AST→IR→契約）では`bind`以外の合成も存在する。**修正案**: `bind`を抽象圏論的合成演算子`∘`に置き換えた一般定理（`preimage_compose_general`）を追加し、`bind`はその特殊ケースとして導出。実例として3層AST変換の合成則適用例を`Examples/`に追加。

**スコア（各10点満点）:**
- 新規性: 4点（型付き設定は明快だが、既存理論との差分が不明瞭）
- 妥当性: 6点（Lean証明は健全だが、実世界への外挿根拠なし）
- 再現性: 8点（Leanビルドは通るが、実プロジェクト評価の手順欠如）
- 可読性: 7点（構成は整理されているが、記法とLean実装の対応に段差）

**総合**: 型理論実装の技術報告としては価値があるが、形式手法の原著論文としては適用範囲の狭さと新規性主張の弱さが致命的。上記3点を修正し、既存枠組みとの理論的区別を明確にした上で再投稿を推奨。
