# 査読報告（査読者3: 形式検証・証明整合性担当）

**Verdict: OK**

## 根拠

### 1. 型定義と定理追跡の整合性
- **本文定義とLean定義の対応**: §2.1記法表により、数学記法 `Ω/β_i/D_i/A_i/f_{0i}` と Lean実装 `α/carrier i/D i/A i/proj i` の対応が明示されている（`paper/lean/UadfU0/Definitions/Model.lean` で確認）
- **逆像定義の型整合**: §2.2の数学的定義 `f^{-1}_{0i}(S)` が `preimage` として型付き実装され、`∃ y, proj i x = some y ∧ y ∈ S` の存在証人形式で機械検証可能になっている
- **順序の一意化**: §2.3で包含順序 `⊆` を定理語彙として固定し、join=`∪`/meet=`∩` の意味を明確化している

### 2. 中核定理の機械検証完備性
- **RQ1〜RQ6対応**: §10結論で各RQに対する定理/ファイル位置が明示されている
  - RQ2（`A⊆D` の持ち上げ）→ `lifted_subset_preimage_domain`, `U0_witness_projects_to_some_domain`
  - RQ3（join/meet分離）→ §4.5 GLB定理群
  - RQ4（層間伝播仮定）→ `lifted_transfer` (同一点連結仮定 `hproj` を含む)
  - RQ5（adequacy分解）→ §4.3 sound-only/complete-only定理
- **仮定の明示性**: §4.1の `lifted_transfer` で関係 `R` だけでなく同一点連結仮定 `hproj` が必要であることを証明構造として残している（設計判断2で説明）
- **部分性の非随伴**: §4.4と `paper/lean/UadfU0/RelatedWork/Galois.lean` で `∃x0, proj x0=none` → left adjoint不成立を構成的に示している

### 3. 論理基盤の健全性
- **外延性原理**: §7.3で `funext`/`propext` のみ使用、`open Classical` なしと明示
- **SpecSet表現**: `α → Prop` による述語ベース集合表現で、`set_ext` による外延的等価性を統一的に扱っている
- **補題階層の分離**: §7.4で中核定理12/背景補題26/具体例5の内訳を示し、証明依存構造の追跡可能性を確保

## Minor notes（将来課題）

1. **抽出器の意味保存**: §6.2の実OSS抽出デモは技術的再実行可能性（RQ6）に焦点を置いており、regex抽出器自体の soundness/completeness 証明は範囲外として明示されている。adequacy定理（§4.3）は抽象関係 `E` に対する理論的枠組みを与えているが、実抽出器への適用は今後課題。
   
2. **colimit一般化**: §8.1で `U0` の colimit 構成への拡張方向を提示しているが、Lean機械化は今後課題として分離されている。現在の証明資産は同一論理空間での join/meet に限定。

3. **`D(i)` 分離の実運用実証**: §2.1で `A(i)⊆D(i)` の理論的必要性を説明し定理として検証しているが、§6.2の抽出デモは `A(i)` 候補生成に主眼があり、`D(i)` 分離の実運用上の有効性評価は今後課題（限界§9に明記）。

4. **`none` 解釈ポリシー**: §4.7で must/may解釈の選択肢を提示しているが、現在の実装は must 寄り（`none` は membership 不成立）に固定している。運用上の偽陽性/偽陰性管理との接続は適用時の宣言事項。

---

**総評**: 本稿は UAD/f 最小コアの参照 mechanization として、定義・定理・仮定の追跡可能性を確保している。型整合性と証明依存構造に致命的不整合はなく、将来課題（抽出器意味保存、colimit、運用実証）は明示的に限界節で分離されている。形式検証・証明整合性の観点から承認可能。
