Verdict: OK

## 根拠3点

1. **論理構造の一貫性（RQ1〜RQ4対応）**  
   - §2で型付きモデル `Model ι α` を定義し、異型層 `carrier : ι → Type w` 上の部分射影 `proj i : α → Option (carrier i)` から逆像 `preimage` を誘導する構成は、Lean実装（`Definitions/Model.lean`）と整合している。
   - `A(i) ⊆ D(i)` の持ち上げ（`lifted_subset_preimage_domain`）、join/meet分離（`U0` vs `UAndOn`）、層間伝播仮定の明示（`lifted_transfer` の `hproj` / `hA`）は、主張範囲内で型整合しており矛盾を生まない。

2. **意味論の二演算分離（RQ3、§3）**  
   - 包含順序 `⊆` を固定し、join（`U0 = ⋃ lifted(i)`）と meet（`UAndOn active`）を別演算として定義し、GLB/LUB性質を定理で示した（§4.5, `Minimality.lean`）ことで、OR統合とAND統合の意味論的混線を回避している。
   - `consistent_iff_exists_UAndOn_pair` により、整合性判定を meet の非空性に接続する構成は論理的に健全である。

3. **仮定明示と再現性（RQ4〜RQ6、§5, §7）**  
   - 層間伝播には同一点連結仮定（`hproj`）が必要であること、部分性で随伴が崩れること（`no_left_adjoint_of_partial`）、抽出適合を one-sided に分解すること（§4.3）を形式的に明示し、Lean build 成功と source-lock 付き抽出デモの再実行可能性を確保した。
   - RQ5/RQ6の境界を明確化し、§6.2の実デモは技術的再実行可能性（RQ6）に焦点を当て、抽出正当性（RQ5）は対象外と分離した点も整合している。

## Minor notes（改善余地、将来課題）

- **限界の明示は適切**（§9）: 抽出器の soundness/completeness 定量評価、母集団代表性、構造制約への拡張は今後課題として明記されており、過剰主張を避けている。
- **記法表の対応**（§2.1）: Lean名 `Ui` と数学記法 `A(i)` の対応は表中に明記されているが、本文中で `Ui` を直接使う箇所（例: §2.2 の Lean抜粋）では読者が一時的に混乱する可能性がある。ただし、この種の記法ズレは編集軽微な範囲である。
- **NL入口管理**（§2.6）: NL→IR→判定の接続点を adequacy で管理する方針は理論的に整合しているが、実運用上の抽出器評価（偽陽性/偽陰性率の定量測定）は含まれていない。これは §9 で明示されており、将来課題として妥当である。
- **must/may解釈**（§4.7）: `proj_i(x)=none` の解釈選択が矛盾判定挙動に影響する点は明記されているが、実デモでの運用ポリシー選択（偽陽性抑制 vs 偽陰性抑制）の具体例は限定的。ただし、数値境界制約のみを対象とした PoC では、この種の判定揺れは構成的負例（§6.4）として示されており、実証範囲は明確である。

以上、致命的矛盾・定義不整合・主張範囲の重大混線はなく、改善余地は編集軽微または将来課題として適切に分離されている。
