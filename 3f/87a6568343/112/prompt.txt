GOAL:
- FM/ITP/CPP/TACAS/FASE/Formal Aspects 投稿基準で採択可否を査読する。

CONSTRAINTS:
- 添付ファイルのみを根拠に評価すること。
- 余計な前提や未提示情報を補わないこと。
- 「査読コメントに沿っているか」ではなく「投稿先基準を満たすか」で判定すること。

TASK:
- Reject/Majorにつながる論点を優先して指摘。
- 定義・定理・仮定・artifact再現性・新規性位置づけを厳密評価。
- 最後に判定を1つ提示。

OUTPUT FORMAT:
- 日本語
- 見出し: 総合判定 / 主要懸念 / 軽微懸念 / 必須修正
- 節番号・定理名・ファイルパスを具体的に示す


# Context files

--- BEGIN FILE: paper/formal-methods/manuscript_fm.md ---
# UAD/f Two-Operator Kernel under Partial Projections: Assumption-Audited Lean4 Mechanization

## Abstract
This paper presents a formal-methods contribution for multi-layer specification comparison under partial projections.
Each layer `i` has its own carrier type `β_i`, and root-to-layer mapping is partial (`proj_i : Ω -> Option β_i`).
On top of this typed kernel we separate two root-side operators with distinct roles:
1. `U0` (join-side coverage baseline)
2. `UAnd` (meet-side conjunctive consistency)

The contribution is an assumption-audited mechanization that makes implicit premises explicit and machine-checkable, with emphasis on failure modes that arise only under partial projections.
We prove, in Lean4, theorems that are technically central in this setting: inter-layer transfer with same-root linkage assumptions, projection composition through `Option.bind` with explicit impossible-branch elimination, one-sided adequacy decomposition (sound/complete split, must/may variants), non-adjointness under partiality, and ideal-root linkage under observability-domain restrictions.
We provide theorem-to-file traceability, explicit proof-obligation interfaces for concrete extractors, and reproducibility metadata.

## 0. Scope, Split, and Quality Bar
This manuscript is the formal-methods paper in a two-paper strategy.
1. Formal paper (this file): typed semantics, theorem statements, mechanized proofs, assumption boundaries.
2. Engineering paper: deterministic replay and operational trust pipeline (`paper/engineering/manuscript_se.md`).

Target quality bar for this paper: FM/ITP/CPP/TACAS/FASE/Formal Aspects.

### 0.1 Claims
This paper claims the following.
1. A self-contained typed UAD/f kernel with explicit partiality (`Option`) and explicit domain/admissibility separation.
2. A semantics-level separation between `U0` (join baseline) and `UAnd` (meet consistency).
3. Assumption-explicit theorems whose validity depends on stated premises, not on hidden intuitions.
4. Lean4 mechanization with theorem-to-file traceability and reproducible artifact checks.

### 0.2 Non-claims
This paper does not claim the following.
1. No proof that a concrete regex or LLM extractor satisfies adequacy assumptions.
2. No statistical external-validity claim from convenience samples.
3. No full temporal/state-transition specification management.
4. No claim that PoC behavior is a direct semantic proof of all formal assumptions.

### 0.3 Boundary alignment with RQ5
`RQ5` in this paper is a theorem-level decomposition over abstract relation `E`.
Concrete extractor correctness remains outside claim boundary.
When instantiated in practice, the paper provides proof-obligation templates, not completed extractor proofs.

## 1. Problem Statement and Research Questions
### 1.1 Problem
In multi-layer engineering settings, artifacts evolve asynchronously.
Even when each layer passes local checks, cross-layer comparison semantics are often implicit.
The central difficulty is that ideal complete root specification `U*` is usually unavailable in practice, while cross-layer comparison still requires a root-side criterion.

The formal direction taken here is:
1. Do not assume complete `U*` as input.
2. Construct root-side operators from partial projections.
3. Keep every non-trivial theorem explicit about its assumptions.

### 1.2 Formal objective
The objective is to provide a minimal typed kernel where:
1. root-side comparison is constructed from partial projections,
2. join-side and meet-side roles are separated,
3. inter-layer and adequacy theorems are proven with explicit hypotheses,
4. all core statements are machine-checked in Lean.

### 1.3 Research questions
1. `RQ1`: Can we define induced inverse images consistently with heterogeneous carriers `carrier : I -> Type`?
2. `RQ2`: Can `A(i) ⊆ D(i)` be lifted into root-side witness validity guarantees?
3. `RQ3`: Can `U0` (join) and `UAnd` (meet) be defined coherently in one model?
4. `RQ4`: Can transfer/composition conditions be typed and mechanized without hidden assumptions?
5. `RQ5`: Can adequacy be decomposed into one-sided forms (sound/complete) over abstract `E`?

Primary RQs for this formal paper are `RQ3`, `RQ4`, and `RQ5`.

### 1.4 Evaluation criteria
1. Definition integrity: type consistency and notation consistency.
2. Theorem integrity: explicit assumptions and machine-checked proofs.
3. Mechanization integrity: no `sorry`, theorem traceability, reproducible build.
4. Claim integrity: clear separation between theorem claims and non-claims.

### 1.5 RQ-to-theorem traceability matrix
| RQ | Answer type | Formal anchor (Lean) | Main section |
|---|---|---|---|
| `RQ1` heterogeneous inverse image | typed definition + typing lemma | `preimage` (definition), `mem_preimage_iff` (`paper/lean/UadfU0/U0Spec/Construction.lean`) | §2.2, §2.3, §3.1 |
| `RQ2` lifting `A(i) ⊆ D(i)` to root witnesses | theorem family | `lifted_subset_preimage_domain`, `U0_witness_projects_to_some_domain` (`paper/lean/UadfU0/U0Spec/Construction.lean`) | §3.1, §3.2 |
| `RQ3` coherent join/meet separation in one model | theorem family | `U0_least_upper_bound_iff`, `UAndOn_greatest_lower_bound_iff`, `UAndOn_subset_U0On`, `UAndOn_empty_eq_univ`, `UAndOn_antitone` | §3.2, §3.3 |
| `RQ4` typed transfer/composition under explicit assumptions | theorem family | `lifted_transfer` (`Transfer.lean`), `preimage_compose` (`Composition.lean`) | §3.4, §3.5 |
| `RQ5` one-sided adequacy decomposition | theorem family | `preimage_subset_semanticPullback_of_sound`, `semanticPullback_subset_preimage_of_complete`, `preimage_eq_semanticPullback`, and may-side analogues (`Adequacy.lean`) | §3.6 |

## 2. Typed Core Model
### 2.1 Notation and order convention
We encode sets as predicates.
- `SpecSet α := α -> Prop`
- this is a local alias in `UadfU0.Definitions.Model`, not a direct use of Mathlib `Set α`

Order is fixed to inclusion `⊆` only.
Under this order:
1. join is union,
2. meet is intersection.

This fixed convention prevents semantic drift across sections.

### 2.2 Layer and model structures
Each layer has both domain and admissible predicates.
- `Layer β`:
  - `D : SpecSet β`
  - `A : SpecSet β`
  - `admissible_subset_domain : A ⊆ D`

Typed UAD/f model:
- `Model ι α`:
  - `carrier : ι -> Type`
  - `layer : (i : ι) -> Layer (carrier i)`
  - `proj : (i : ι) -> α -> Option (carrier i)`

This enforces per-layer heterogeneity (`β_i` may differ by index).

Admissible-set alias used in theorem signatures:
- `Ui(i) := A(i)` (compatibility alias)
- in Lean this is `def Ui (M : Model ι α) (i : ι) := M.A i`
- throughout this paper, `Ui` and `A` denote the same predicate at layer `i`

### 2.3 Lean definition excerpt (compilable context)
```lean
namespace UadfU0
namespace Model

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

def preimage (i : ι) (S : SpecSet (M.carrier i)) : SpecSet α :=
  fun x => ∃ y : M.carrier i, M.proj i x = some y ∧ y ∈ S

def Ui (i : ι) : SpecSet (M.carrier i) :=
  M.A i

def lifted (i : ι) : SpecSet α :=
  M.preimage i (M.Ui i)

def U0On (active : ι → Prop) : SpecSet α :=
  fun x => ∃ i, active i ∧ x ∈ M.lifted i

def UAndOn (active : ι → Prop) : SpecSet α :=
  fun x => ∀ i, active i → x ∈ M.lifted i

end Model
end UadfU0
```
(Definitions in `paper/lean/UadfU0/U0Spec/Construction.lean`.)

### 2.4 Induced inverse image and lifted sets (must semantics)
For each layer `i`:

\[
preimage_i(S) := \{x \in Ω \mid \exists y \in β_i,\ proj_i(x)=some(y) \land y \in S\}
\]

Lifted admissible predicate:

\[
lifted(i) := preimage_i(A(i))
\]

### 2.5 Root-side operators
Active-restricted operators:

\[
U0On(active) := \{x \mid \exists i,\ active(i) \land x \in lifted(i)\}
\]
\[
UAndOn(active) := \{x \mid \forall i,\ active(i) \to x \in lifted(i)\}
\]

Global forms:

\[
U0 := U0On(\lambda _, True),\quad UAnd := UAndOn(\lambda _, True)
\]

Interpretation under `⊆`:
1. `U0` is join-side least upper bound.
2. `UAndOn` is meet-side greatest lower bound.

### 2.6 May extension
Must excludes undefined projections from membership.
May includes undefined projections as inconclusive-allowed branch:

\[
preimageMay_i(S) := \{x \mid proj_i(x)=none \lor \exists y,\ proj_i(x)=some(y) \land y \in S\}
\]

Derived may operators:
- `liftedMay`
- `UAndMayOn`

Mechanized interface boundary:
1. must and may equalities are proved in separate theorem families (`preimage_eq_semanticPullback` vs `preimageMay_eq_semanticPullbackMay`),
2. cross-family relation is explicit only through named inclusion theorems such as `preimage_subset_preimageMay` and `UAndOn_subset_UAndMayOn`.

### 2.7 Pairwise consistency predicates
\[
Consistent(i,j) :\Leftrightarrow \exists x,\ x\in lifted(i) \land x\in lifted(j)
\]
\[
Contradictory(i,j) :\Leftrightarrow \forall x,\ x\in lifted(i) \to x\in lifted(j) \to False
\]

(Lean: `paper/lean/UadfU0/InterLayer/Consistency.lean`.)

### 2.8 Root-space instantiation templates
We distinguish two conceptual root spaces.
1. `Ω_trace`: event/trace-level root space.
2. `Ω_art`: artifact-bundle root space.

For extractor-aware decomposition:
1. `obs_i : Ω -> Option Γ_i`
2. `extract_i : Γ_i -> Option β_i`
3. `proj_i := Option.bind (obs_i) extract_i`

The formal theorems are stated over abstract `proj_i`; this decomposition is a bridge template, not a hidden assumption.

## 3. Main Mechanized Results
### 3.1 Foundational lemmas (supporting, not novelty claims)
From `paper/lean/UadfU0/U0Spec/Construction.lean`:
1. `preimage_monotone`
2. `preimage_union`
3. `preimage_subset_preimageMay`
4. `lifted_subset_liftedMay`
5. `lifted_subset_preimage_domain`

These are explicitly treated as support lemmas.

### 3.2 Join/meet characterizations
From `paper/lean/UadfU0/U0Spec/Minimality.lean`:
1. `U0_least_upper_bound_iff`
2. `UAndOn_greatest_lower_bound_iff`

Interpretation:
1. `U0` is the LUB of lifted family under `⊆`.
2. `UAndOn(active)` is the GLB of active lifted family under `⊆`.

Explicit RQ linkage:
1. `RQ1` is answered by typed inverse-image definitions parameterized by heterogeneous `carrier : ι -> Type`.
2. `RQ2` is answered by `lifted_subset_preimage_domain` and `U0_witness_projects_to_some_domain`, which propagate `A(i) ⊆ D(i)` to root witnesses.

### 3.3 Central role-separation result for `RQ3`
From `paper/lean/UadfU0/U0Spec/Construction.lean`:
1. `U0On_monotone`
2. `UAndOn_antitone`
3. `UAndOn_subset_U0On` (requires non-empty active set)
4. `consistent_iff_exists_UAndOn_pair`
5. `UAndOn_empty_eq_univ`

Implication:
1. join-side and meet-side operators are behaviorally distinct,
2. active-layer expansion has opposite monotonicity effects,
3. vacuous-truth edge cases are explicit.

Role-separation theorem bundle (paper-level statement):
1. `U0On` is monotone in active-set growth, while `UAndOn` is antitone.
2. Under non-empty active sets, `UAndOn(active) ⊆ U0On(active)`.
3. Under empty active set, `UAndOn` collapses to universal predicate (`UAndOn_empty_eq_univ`), so any consistency interpretation must explicitly enforce `∃ i, active i`.

Vacuous-truth implication:
1. `UAndOn_empty_eq_univ` means meet over zero active layers yields universal predicate,
2. therefore any operational interpretation of `UAndOn` must enforce non-empty active sets when using it as a consistency signal.

Difference from total-map-only intuition:
1. In total-map settings, undefined-observation branch does not exist and must/may collapse.
2. In this partial kernel, strict inclusion can appear (`preimage_subset_preimageMay`, `UAndOn_subset_UAndMayOn`), so join/meet reasoning must be paired with explicit undefined-point policy.
3. Therefore `U0`/`UAnd` separation here is not only order-theoretic; it is policy-sensitive under partiality.

### 3.4 Inter-layer transfer theorem
Formal theorem (`InterLayer/Transfer.lean`):

\[
(\forall x,\forall y_j,\ proj_j(x)=some(y_j) \to \exists y_i,\ proj_i(x)=some(y_i) \land R(y_i,y_j))
\]
\[
\land\ (\forall y_i,y_j,\ R(y_i,y_j) \to y_j\in A(j) \to y_i\in A(i))
\]
\[
\Rightarrow lifted(j) \subseteq lifted(i)
\]

Lean signature excerpt:
```lean
theorem lifted_transfer
    (i j : ι)
    (R : M.carrier i → M.carrier j → Prop)
    (hproj :
      ∀ x : α, ∀ yj : M.carrier j,
        M.proj j x = some yj →
        ∃ yi : M.carrier i, M.proj i x = some yi ∧ R yi yj)
    (hA :
      ∀ yi : M.carrier i, ∀ yj : M.carrier j,
        R yi yj → yj ∈ M.Ui j → yi ∈ M.Ui i)
    : M.lifted j ⊆ M.lifted i
```

Proof skeleton:
1. Take arbitrary `x ∈ lifted(j)`.
2. Extract witness `yj` with `proj_j x = some yj` and `yj ∈ A(j)`.
3. Apply `hproj` at the same root point `x` to get `yi` and `proj_i x = some yi` with `R yi yj`.
4. Apply `hA` to transfer admissibility from `A(j)` to `A(i)`.
5. Construct witness for `x ∈ lifted(i)`.

Assumption audit:
1. Without same-root linkage (`hproj`), codomain relation `R` alone is insufficient.
2. Without admissibility-preservation (`hA`), transfer of set membership fails.

### 3.5 Composition theorem for induced inverse images
Formal theorem (`InterLayer/Composition.lean`):

\[
proj_j(x)=Option.bind(proj_i(x),g)
\Rightarrow
preimage_j(S)=preimage_i(pullbackVia_g(S))
\]

with

\[
pullbackVia_g(S)=\{y_i\mid \exists y_j,\ g(y_i)=some(y_j)\land y_j\in S\}
\]

Lean signature excerpt:
```lean
theorem preimage_compose
    (i j : ι)
    (g : M.carrier i → Option (M.carrier j))
    (hcomm : ∀ x, M.proj j x = Option.bind (M.proj i x) g)
    (S : SpecSet (M.carrier j))
    : M.preimage j S = M.preimage i (M.pullbackVia g S)
```

Proof skeleton (both directions explicit):
1. `j -> i` direction:
2. Start from witness `yj` in `preimage_j(S)`.
3. Use `hcomm` and case analysis on `proj_i x`.
4. In `none` branch, `Option.bind (proj_i x) g = none`, contradicting `proj_j x = some yj` via `hcomm`.
5. In `some yi` branch, reduce `Option.bind (some yi) g = some yj` to `g yi = some yj` and construct pullback witness.
6. `i -> j` direction:
7. Start from witness `yi` plus pullback witness `yj`.
8. Rebuild `Option.bind` equality and conclude `proj_j x = some yj`.

Why non-trivial in this model:
1. proof depends on partiality-aware branch handling,
2. impossible branch elimination is essential and mechanized.

### 3.6 One-sided adequacy decomposition (must and may)
From `paper/lean/UadfU0/InterLayer/Adequacy.lean`.

Motivation for introducing abstract `E`:
1. `proj` captures an implementation-level observation path,
2. `E` captures semantic extraction relation used for adequacy statements,
3. separating them allows theorem-level reasoning before committing to a concrete extractor implementation.

Definitions:
\[
semanticPullback(E,S):=\{x\mid \exists y, E(x,y)\land y\in S\}
\]
\[
semanticPullbackMay(E,S):=\{x\mid proj_i(x)=none\ \lor\ \exists y, E(x,y)\land y\in S\}
\]

Must-side theorems:
1. `preimage_subset_semanticPullback_of_sound`
2. `semanticPullback_subset_preimage_of_complete`
3. `preimage_eq_semanticPullback`

May-side theorems:
1. `preimageMay_subset_semanticPullbackMay_of_sound`
2. `semanticPullbackMay_subset_preimageMay_of_complete`
3. `preimageMay_eq_semanticPullbackMay`

Interpretation boundary:
1. These are abstract theorems over relation `E`.
2. Extractor-specific correctness is not proven here.

#### 3.6.1 Concrete-extractor proof obligations (template)
For a concrete extractor decomposition:
1. `obs_i : Ω -> Option Γ_i`
2. `extract_i : Γ_i -> Option β_i`
3. `proj_i := Option.bind obs_i extract_i`

typical sufficient obligations are:
1. Sound side:
   - `obs_i x = some γ ∧ extract_i γ = some y -> E_extract x y`
2. Complete side:
   - `E_extract x y -> ∃ γ, obs_i x = some γ ∧ extract_i γ = some y`
3. none-policy side:
   - must or may handling of inconclusive is fixed as an operational policy.

Lean mapping:
1. sound obligation corresponds to `hSound`.
2. complete obligation corresponds to `hComplete`.
3. adequacy application reads as substitution `E := E_extract`.

#### 3.6.2 Why `semanticPullbackMay` contains `proj`-none branch
May-comparison requires matching the same inconclusive policy on both sides.
Therefore `semanticPullbackMay` includes `proj_i(x)=none` branch explicitly.
This is an operational comparability choice, not an arbitrary mixing of abstractions.

### 3.7 Non-adjointness under partial projections
From `paper/lean/UadfU0/RelatedWork/Galois.lean`:

Adjointness notion used:
\[
HasLeftAdjoint(G) :\Leftrightarrow \exists F,\ \forall S,T,\ F(S)\subseteq T \leftrightarrow S\subseteq G(T)
\]
with `G = preimage_i`.

\[
(\exists x_0,\ proj_i(x_0)=none) \Rightarrow \neg HasLeftAdjoint(preimage_i)
\]

Lean signature excerpt:
```lean
theorem no_left_adjoint_of_partial
    (M : Model ι α) (i : ι) (x0 : α)
    (hnone : M.proj i x0 = none)
    : ¬ HasLeftAdjoint (M.preimage i)
```

Proof skeleton:
1. Assume a left adjoint `F` exists.
2. Instantiate adjunction with singleton root set `S = {x0}` and universal codomain set `T = ⊤`.
3. From left side (`F S ⊆ T` trivial), derive right side `S ⊆ preimage_i(T)`.
4. Hence `x0 ∈ preimage_i(T)` and there exists `y` with `proj_i x0 = some y`.
5. Contradiction with `proj_i x0 = none`.

This theorem blocks silent import of total-map Galois intuitions into partial-projection settings.
In this paper the theorem is a mechanized guardrail: any adjunction-based reasoning must explicitly justify totality or equivalent conditions.

### 3.8 Ideal-root linkage under observability domains
From `paper/lean/UadfU0/U0Spec/IdealRoot.lean`:
1. `UStar : SpecSet α` is an explicit theorem parameter denoting ideal-root predicate
   (it is not constructed by this kernel; theorem statements are parametric in `UStar`)
2. `projDom(i) := {x | ∃ y, proj_i x = some y}`
3. `projDomOn(active) := {x | ∀ i, active i -> x ∈ projDom(i)}`

Representative Lean signature:
```lean
theorem UStar_inter_projDomOn_subset_UAndOn
    {active : ι → Prop}
    (UStar : SpecSet α)
    (hNecessaryOnDom :
      ∀ i : ι, active i →
        (fun x : α => x ∈ UStar ∧ x ∈ M.projDom i) ⊆ M.lifted i) :
    (fun x : α => x ∈ UStar ∧ x ∈ M.projDomOn active) ⊆ M.UAndOn active
```

Key theorems:
1. `UStar_inter_projDomOn_subset_UAndOn`
2. `UStar_inter_projDomOn_subset_U0On_of_nonempty_active`
3. `UStar_subset_UAndMayOn`

Interpretation:
1. under partial observability, ideal-root linkage requires domain-restricted assumptions,
2. may-side linkage and must-side linkage are not interchangeable.
3. because `UStar` is parametric input, these are conditional linkage theorems rather than a construction of `UStar`.

## 4. Mechanization Method and Proof Integrity
### 4.1 Mechanization principles
1. Every theorem states assumptions in argument list.
2. No implicit classical package opening in core files.
3. No empirical evidence is used as theorem proof evidence.
4. Edge cases (empty active set, undefined projection points) are explicitly theoremized.

### 4.2 Proof-structure discipline
Selected proof styles are intentionally explicit.
1. witness extraction and reconstruction for existential sets,
2. extensional equality proofs via `set_ext`,
3. branch analysis on `Option` where partiality matters,
4. subset reasoning through compositional lemmas (`subset_trans`, monotonicity theorems).

### 4.3 Logic basis disclosure
1. toolchain: `leanprover/lean4:v4.27.0`.
2. manifest hash (`paper/lean/lake-manifest.json`):
   - `8c098d788704fb7c279c7004a1f492723bd892acf2500483665ae39e7a00a6e7`
3. extensional equality reasoning uses `funext` and `propext`.
4. `paper/lean/UadfU0` does not use `open Classical`, but some proofs still depend on core axioms through proof terms.
5. Axiom audit (`#print axioms`) on core theorems:
   - `lifted_transfer`: no axioms
   - `preimage_compose`: `[propext, Classical.choice, Quot.sound]`
   - `preimage_eq_semanticPullback`: `[propext, Quot.sound]`
   - `preimageMay_eq_semanticPullbackMay`: `[propext, Quot.sound]`
   - `no_left_adjoint_of_partial`: `[propext]`
   - `UStar_inter_projDomOn_subset_UAndOn`: no axioms
6. `Classical.choice` appears in some equality proofs (for example `preimage_compose`) because witness reconstruction is expressed through extensional equality over existential branches; this paper does not claim constructivity-preserving normalization for those proofs.

### 4.4 Artifact integrity checks
```bash
cd paper/lean
~/.elan/bin/lake build
rg -n '^theorem ' UadfU0 | wc -l
rg -n '\bsorry\b' UadfU0 || true
wc -l $(rg --files UadfU0 | tr '\n' ' ') | tail -n 1
```

Current values:
1. theorem count: `59`
2. `sorry` count: `0`
3. LOC (`paper/lean/UadfU0`): `1502`

Artifact scope in this submission:
1. full Lean sources under `paper/lean/UadfU0` are part of the formal artifact package,
2. theorem catalog and assumption matrix are derived from those sources.

### 4.5 Theorem-to-file traceability
| Formal claim | Lean theorem(s) | File |
|---|---|---|
| must/may operator laws | `U0On_monotone`, `UAndOn_antitone`, `UAndOn_subset_U0On`, `UAndOn_subset_UAndMayOn` | `paper/lean/UadfU0/U0Spec/Construction.lean` |
| join/meet characterization | `U0_least_upper_bound_iff`, `UAndOn_greatest_lower_bound_iff` | `paper/lean/UadfU0/U0Spec/Minimality.lean` |
| transfer | `lifted_transfer` | `paper/lean/UadfU0/InterLayer/Transfer.lean` |
| composition | `preimage_compose` | `paper/lean/UadfU0/InterLayer/Composition.lean` |
| adequacy (must) | `preimage_subset_semanticPullback_of_sound`, `semanticPullback_subset_preimage_of_complete`, `preimage_eq_semanticPullback` | `paper/lean/UadfU0/InterLayer/Adequacy.lean` |
| adequacy (may) | `preimageMay_subset_semanticPullbackMay_of_sound`, `semanticPullbackMay_subset_preimageMay_of_complete`, `preimageMay_eq_semanticPullbackMay` | `paper/lean/UadfU0/InterLayer/Adequacy.lean` |
| consistency/contradiction duality | `contradictory_iff_not_consistent` | `paper/lean/UadfU0/InterLayer/Consistency.lean` |
| non-adjointness under partiality | `no_left_adjoint_of_partial` | `paper/lean/UadfU0/RelatedWork/Galois.lean` |
| ideal-root observability-domain linkage | `UStar_inter_projDomOn_subset_UAndOn`, `UStar_inter_projDomOn_subset_U0On_of_nonempty_active`, `UStar_subset_UAndMayOn` | `paper/lean/UadfU0/U0Spec/IdealRoot.lean` |

## 5. Mechanized Examples (Formal Sanity Layer)
### 5.1 Artifact bundle instantiation
File: `paper/lean/UadfU0/Examples/ArtifactBundleExample.lean`.

What is mechanized:
1. concrete `Ω_art` bundle type,
2. `obs/extract/proj=bind` decomposition,
3. layer-level domain/admissibility predicates,
4. witness-level examples for `U0`, `UAnd`, and must/may divergence.
5. explicit heterogeneous carriers (`ReqIR`, `ApiIR`, `CodeIR`) under one index type, demonstrating RQ1 in a concrete model instance.

This example validates model instantiation consistency, not external validity.

### 5.2 Password-policy case theorem
File: `paper/lean/UadfU0/CaseStudy/PasswordPolicy.lean`.

Proven statement:
1. closed-form consistency check is equivalent to existence of shared witness across three lifted layers (`checkConsistent_iff_allThree`).

This is used as a mechanized sanity theorem for constrained interval domain.

### 5.3 Why these examples matter for the formal paper
1. They verify that theorem interfaces are usable in concrete typed instantiations.
2. They expose none/must/may behavior in executable proof contexts.
3. They do not replace general proofs and are not used to claim broad engineering effectiveness.
4. They provide assumption-level debugging hooks (for example, same-root linkage and bind-commutation requirements are visible at executable model level).

## 6. What Mechanization Added Beyond Textbook Identities
This paper explicitly distinguishes classical overlap from mechanization-specific value.

Classical overlap (not novelty claims):
1. inverse-image monotonicity,
2. LUB/GLB identities under inclusion,
3. contradiction/non-consistency duality.

Mechanization-specific value in this UAD/f kernel:
1. assumption surfacing:
   - same-root linkage (`hproj`) and admissibility transport (`hA`) are explicit.
2. partiality discipline:
   - `none` branch is first-class and theorem-visible.
3. one-sided adequacy granularity:
   - sound/complete can be reasoned separately before equivalence.
4. adjunction caution:
   - partiality invalidates naive total-map adjoint intuitions.
5. observability-domain linkage:
   - ideal-root claims are domain-restricted rather than over-assumed.

Per-theorem hardness summary:
1. `lifted_transfer` is not a set-theory tautology because same-root witness linkage is required and mechanized explicitly.
2. `preimage_compose` is not immediate under partiality because impossible `none` branches must be eliminated constructively.
3. adequacy equalities are not free: they hold only when one-sided obligations are paired; one-sided failures remain explicit.
4. non-adjointness exhibits a partiality-specific structural break that blocks naive reuse of total-map adjoint intuitions.
5. ideal-root linkage requires observability-domain restriction to avoid overclaiming under undefined projections.

### 6.1 Concrete delta against baseline theorem libraries
This subsection states the delta at definition/theorem-interface level.

Baseline capabilities (available in standard theorem-library practice):
1. total-function preimage monotonicity and union laws,
2. lattice-level LUB/GLB facts under inclusion.

What this kernel adds as one auditable package:
1. one model combining heterogeneous carriers (`carrier : ι -> Type`) with partial projections (`Option`) and domain/admissibility separation,
2. explicit transfer/composition assumptions as theorem arguments (`hproj`, `hA`, `hcomm`) rather than implicit prose assumptions,
3. adequacy decomposition in both must/may forms with named bridge theorems,
4. explicit non-adjointness boundary theorem under undefined points,
5. assumption matrix and theorem catalog linked to concrete Lean files.

Claim boundary for this delta:
1. we do not claim these mathematical identities are new,
2. we claim this specific partial/heterogeneous/assumption-audited kernel is packaged and machine-checked end-to-end.

## 7. Relation to Prior Work
### 7.1 Abstract interpretation and order-theoretic semantics
Abstract interpretation gives language for monotonic abstractions.
Our delta is explicit partial-projection (`Option`) mechanization with assumption-level auditing.

### 7.2 Institutions and heterogeneous semantics
Institutional frameworks capture satisfaction transfer across logics.
Our scope is narrower: a typed root-operator kernel with machine-checked assumptions under partial projections.

### 7.3 View consistency and inconsistency governance
View-consistency literature motivates multi-view comparison problems.
Our contribution is a formal kernel fixing operator semantics and assumption boundaries, not a full governance workflow.

### 7.4 BX/synchronization lines
BX and synchronization works treat propagation and consistency restoration.
Our transfer/composition theorems isolate minimal one-way reverse-mapping conditions in an assumption-audited Lean setting.

### 7.5 Galois-connection mechanization lines
Constructive and mechanized Galois-connection literature motivates careful treatment of adjunction assumptions.
Our non-adjointness result is positioned as a partiality-specific boundary theorem inside this line, not as a replacement of that literature.

### 7.6 Positioning statement
This paper is intended as a mechanized kernel contribution:
1. not a replacement for institution/BX frameworks,
2. not a complete temporal semantics framework,
3. but a reusable formal core with explicit assumptions and machine-checkable proofs.

### 7.7 Definition-level differences from two nearby lines
Difference to institution-style frameworks:
1. this paper does not mechanize general signature/sentence/model morphisms with satisfaction condition preservation,
2. instead it fixes one typed root-kernel with partial projections and proves operator/transfer/adequacy results inside that kernel.

Difference to BX/TGG synchronization lines:
1. this paper does not mechanize bidirectional restoration algorithms,
2. instead it proves one-way reverse-mapping conditions (`lifted_transfer`, `preimage_compose`) with explicit hypotheses and failure-mode boundaries under partiality.

Difference to total-map set/order formalizations:
1. undefined projection points are first-class (`Option`) and may alter theorem interfaces (must/may split),
2. `U0`/`UAnd` analysis is coupled with undefined-point policy rather than only total preimage algebra.

## 8. Limitations and Threats to Validity
1. Constraint language focus is parameter-bound centric.
2. Temporal/state-transition semantics are out of scope.
3. Extractor-specific adequacy proofs are external obligations.
4. Engineering replay evidence is not theorem evidence.
5. If some layer is overly permissive, `U0` may become coarse as a coverage baseline.

## 9. Reproducibility Pointers
Included formal sources (submission package):
1. `paper/lean/UadfU0/Definitions`
2. `paper/lean/UadfU0/U0Spec`
3. `paper/lean/UadfU0/InterLayer`
4. `paper/lean/UadfU0/RelatedWork`
5. `paper/lean/UadfU0/Examples`
6. `paper/lean/UadfU0/CaseStudy`

Build entry:
```bash
cd paper/lean
~/.elan/bin/lake build
```

Expected integrity outcomes:
1. build succeeds (`Build completed successfully`),
2. theorem declarations in `UadfU0` (from `paper/lean` working directory) match `paper/formal-methods/appendix/theorem_catalog.md`,
3. `rg -n '\\bsorry\\b' UadfU0` returns no matches.

Verification scope declaration:
1. claims in §4.4 and §9 are limited to artifacts included in this submission package (`paper/lean/UadfU0`, manifest, and appendices),
2. no external benchmark, network source, or engineering-pipeline output is used as formal-proof evidence in this manuscript.

Submission package pointers:
1. `paper/formal-methods/appendix/fm_submission_checklist.md`
2. `paper/formal-methods/appendix/theorem_catalog.md`
3. `paper/formal-methods/appendix/assumption_matrix.md`

## References (selection)
1. P. Cousot and R. Cousot. Abstract Interpretation: A Unified Lattice Model... POPL 1977.
2. J. Goguen and R. Burstall. Introducing Institutions. LNCS 164, 1984.
3. L. Lamport. *Specifying Systems*. Addison-Wesley, 2002.
4. L. de Alfaro and T. Henzinger. Interface Automata. FSE 2001.
5. R.-J. Back and J. von Wright. *Refinement Calculus*. Springer, 1998.
6. B. Nuseibeh, J. Kramer, A. Finkelstein. ViewPoints. IEEE TSE, 1994.
7. M. Wirsing and A. Knapp. View Consistency in Software Development. LNCS, 2004.
8. Y. Xiong et al. Model Synchronization based on Triple Graph Grammars. SoSyM, 2012.
9. T. Mossakowski et al. Heterogeneous Tool Set (Hets), CEUR-WS.
10. L. de Moura and S. Ullrich. The Lean 4 Theorem Prover and Programming Language. CADE, 2021.
11. A. Darais and D. Van Horn. Constructive Galois Connections. ICFP, 2016.
--- END FILE: paper/formal-methods/manuscript_fm.md ---

--- BEGIN FILE: paper/formal-methods/appendix/assumption_matrix.md ---
# Assumption Matrix (Core Theorems and Supporting Lemmas)

This appendix summarizes which assumptions each core theorem needs, and what fails if an assumption is removed.

| Theorem or lemma | Required assumptions | Failure mode if removed | Fallback claim | Lean reference |
|---|---|---|---|---|
| `lifted_transfer` | `hproj` (same-root linkage), `hA` (admissibility transport) | Without `hproj`, codomain relation alone cannot lift root witness across layers. Without `hA`, witness does not transfer from `A(j)` to `A(i)`. | No general subset transfer claim; only layer-local lifted predicates remain. | `paper/lean/UadfU0/InterLayer/Transfer.lean` |
| `preimage_compose` | `hcomm : proj_j = bind(proj_i,g)` pointwise | Equality of preimages cannot be derived. Without `hcomm`, the `none` branch cannot be eliminated via bind commutation, so equality reduces to weaker one-sided reasoning only. | Keep separate one-directional implications only when independently proven. | `paper/lean/UadfU0/InterLayer/Composition.lean` |
| `preimage_eq_semanticPullback` | both sound and complete directions for `E` | Only one-sided inclusion is derivable; equality is not justified. | Use `preimage_subset_semanticPullback_of_sound` or `semanticPullback_subset_preimage_of_complete` only. | `paper/lean/UadfU0/InterLayer/Adequacy.lean` |
| `preimageMay_eq_semanticPullbackMay` | may-side sound and complete directions | May-side analogue of must case: equality collapses to one-sided relation only if one direction is missing. | Use `preimageMay_subset_semanticPullbackMay_of_sound` or `semanticPullbackMay_subset_preimageMay_of_complete` only. | `paper/lean/UadfU0/InterLayer/Adequacy.lean` |
| `UAndOn_subset_U0On` | non-empty active set (`∃ i, active i`) | For empty active set, `UAndOn` becomes the universal set (`univ`), so subset to `U0On` is not generally valid. | Restrict to non-empty active sets; for empty active set use `UAndOn_empty_eq_univ` explicitly. | `paper/lean/UadfU0/U0Spec/Construction.lean` |
| `no_left_adjoint_of_partial` | existence of undefined projection point (`proj_i x0 = none`) | Scope note: theorem applies under partiality. For total projections, this theorem makes no non-adjointness claim. | None in general; total case requires separate proof obligations. | `paper/lean/UadfU0/RelatedWork/Galois.lean` |
| `UStar_inter_projDomOn_subset_UAndOn` | domain-restricted necessary-condition premise | Ideal-root inclusion over full space is not justified under partial observability. | Keep domain-restricted claim over `UStar ∩ projDomOn(active)` only. | `paper/lean/UadfU0/U0Spec/IdealRoot.lean` |

Practical reading rule:
1. if all assumptions hold, use the theorem claim exactly as stated,
2. if any assumption fails or is unproven, fall back to the weaker variant noted in the fallback-claim column (usually one-sided inclusions).
--- END FILE: paper/formal-methods/appendix/assumption_matrix.md ---

--- BEGIN FILE: paper/formal-methods/appendix/theorem_catalog.md ---
# Theorem Catalog (Lean Mechanization)

This appendix lists theorem declarations in `paper/lean/UadfU0` for traceability.

- Total theorem declarations: `59`

## `paper/lean/UadfU0/CaseStudy/PasswordPolicy.lean`

- theorem count: `4`

- `req_projection_adequacy`
- `checkConsistent_true_implies_allThree`
- `allThree_implies_checkConsistent_true`
- `checkConsistent_iff_allThree`

## `paper/lean/UadfU0/Definitions/Model.lean`

- theorem count: `3`

- `subset_refl`
- `subset_trans`
- `set_ext`

## `paper/lean/UadfU0/Examples/ArtifactBundleExample.lean`

- theorem count: `1`

- `proj_bind_decomposition`

## `paper/lean/UadfU0/Examples/CompositionExample.lean`

- theorem count: `1`

- `proj_compose_holds`

## `paper/lean/UadfU0/Examples/ContradictoryLayers.lean`

- theorem count: `1`

- `contradictoryModel_is_contradictory`

## `paper/lean/UadfU0/Examples/TransferChainExample.lean`

- theorem count: `2`

- `transfer_code_to_api`
- `transfer_api_to_req`

## `paper/lean/UadfU0/Examples/TransferExample.lean`

- theorem count: `1`

- `transfer_false_to_true`

## `paper/lean/UadfU0/InterLayer/Adequacy.lean`

- theorem count: `6`

- `preimage_subset_semanticPullback_of_sound`
- `semanticPullback_subset_preimage_of_complete`
- `preimage_eq_semanticPullback`
- `preimageMay_subset_semanticPullbackMay_of_sound`
- `semanticPullbackMay_subset_preimageMay_of_complete`
- `preimageMay_eq_semanticPullbackMay`

## `paper/lean/UadfU0/InterLayer/Composition.lean`

- theorem count: `2`

- `preimage_compose`
- `lifted_subset_of_compose`

## `paper/lean/UadfU0/InterLayer/Consistency.lean`

- theorem count: `3`

- `contradictory_iff_not_consistent`
- `consistent_implies_U0_nonempty`
- `contradictory_implies_no_shared_witness`

## `paper/lean/UadfU0/InterLayer/Transfer.lean`

- theorem count: `2`

- `lifted_transfer`
- `consistent_transport_left`

## `paper/lean/UadfU0/RelatedWork/Galois.lean`

- theorem count: `1`

- `no_left_adjoint_of_partial`

## `paper/lean/UadfU0/U0Spec/Construction.lean`

- theorem count: `19`

- `mem_preimage_iff`
- `preimage_monotone`
- `preimage_union`
- `preimage_subset_preimageMay`
- `lifted_subset_liftedMay`
- `lifted_subset_preimage_domain`
- `mem_U0_iff`
- `U0On_monotone`
- `U0_eq_U0On_all`
- `UAnd_eq_UAndOn_all`
- `UAndOn_antitone`
- `UAndOn_subset_UAndMayOn`
- `UAndMayOn_empty_implies_UAndOn_empty`
- `UAndOn_empty_eq_univ`
- `UAndOn_subset_U0On`
- `consistent_iff_exists_UAndOn_pair`
- `lifted_subset_U0`
- `U0_nonempty_of_exists_layer_nonempty`
- `U0_witness_projects_to_some_domain`

## `paper/lean/UadfU0/U0Spec/IdealRoot.lean`

- theorem count: `6`

- `UStar_subset_UAndOn`
- `UStar_inter_projDomOn_subset_UAndOn`
- `UStar_subset_U0On_of_nonempty_active`
- `UStar_inter_projDomOn_subset_U0On_of_nonempty_active`
- `UStar_subset_UAndMayOn`
- `UStar_subset_UAnd`

## `paper/lean/UadfU0/U0Spec/Minimality.lean`

- theorem count: `7`

- `U0_upper_bound`
- `U0_below_every_upper_bound`
- `U0_least_upper_bound_iff`
- `U0_is_supremum`
- `UAndOn_lower_bound`
- `below_UAndOn_of_lower_bounds`
- `UAndOn_greatest_lower_bound_iff`
--- END FILE: paper/formal-methods/appendix/theorem_catalog.md ---

--- BEGIN FILE: paper/formal-methods/appendix/fm_submission_checklist.md ---
# FM Submission Checklist

## Artifact Integrity
- [x] `lake build` succeeds on clean checkout
- [x] theorem-to-file mapping in manuscript matches current Lean files
- [x] no hidden assumptions in theorem text (major hypotheses are named)
- [x] no claim that empirical PoC validates adequacy theorems directly
- [x] theorem-assumption matrix is published (`appendix/assumption_matrix.md`)

## Logical Scope
- [x] order convention fixed (inclusion only)
- [x] `U0`/`UAnd` roles separated and stable
- [x] must/may comparisons are not cross-mixed
- [x] non-adjointness statement scoped to partial projection setting

## Reproducibility Metadata
- [x] Lean toolchain version pinned (`paper/lean/lean-toolchain`)
- [x] build entry command documented
- [x] proof assets listed with stable paths
- [x] manifest hash disclosed
- [x] theorem catalog published (`paper/formal-methods/appendix/theorem_catalog.md`)
- [x] axiom-audit command and outputs summarized in manuscript

## Positioning
- [x] explicitly states classical overlap (LUB/GLB, pullback)
- [x] explicitly states mechanization-specific contribution (assumption audit + partiality)
- [x] no overclaim of mathematical novelty

## Suggested Verification Commands
```bash
cd paper/lean
~/.elan/bin/lake build
rg -n '^theorem ' UadfU0 | wc -l
rg -n '\bsorry\b' UadfU0 || true
```
--- END FILE: paper/formal-methods/appendix/fm_submission_checklist.md ---

--- BEGIN FILE: paper/lean/UadfU0/CaseStudy/PasswordPolicy.lean ---
import UadfU0.InterLayer.Adequacy

namespace UadfU0
namespace CaseStudy

open Model

inductive LayerIx where
  | req
  | api
  | code
deriving DecidableEq, Repr

structure ReqArtifact where
  minLen : Nat
  maxLen : Nat

structure ApiArtifact where
  minLen : Nat

structure CodeArtifact where
  maxLen : Nat

def reqAdmissible (r : ReqArtifact) : SpecSet Nat :=
  fun n => r.minLen ≤ n ∧ n ≤ r.maxLen

def apiAdmissible (a : ApiArtifact) : SpecSet Nat :=
  fun n => a.minLen ≤ n

def codeAdmissible (c : CodeArtifact) : SpecSet Nat :=
  fun n => n ≤ c.maxLen

abbrev passwordModel (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact) :
    Model LayerIx Nat where
  carrier := fun _ => Nat
  layer
  | .req =>
      {
        D := fun _ : Nat => True
        A := reqAdmissible r
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | .api =>
      {
        D := fun _ : Nat => True
        A := apiAdmissible a
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | .code =>
      {
        D := fun _ : Nat => True
        A := codeAdmissible c
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  proj _ n := some n

def extractedLower (r : ReqArtifact) (a : ApiArtifact) : Nat :=
  Nat.max r.minLen a.minLen

def extractedUpper (r : ReqArtifact) (c : CodeArtifact) : Nat :=
  Nat.min r.maxLen c.maxLen

def checkConsistent (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact) : Bool :=
  decide (extractedLower r a ≤ extractedUpper r c)

def allThreeConsistent (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact) : Prop :=
  let M := passwordModel r a c
  ∃ n : Nat,
    n ∈ M.lifted .req ∧ n ∈ M.lifted .api ∧ n ∈ M.lifted .code

def reqExtractRel (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact) :
    Nat → (passwordModel r a c).carrier .req → Prop :=
  fun x y => y = x

theorem req_projection_adequacy
    (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact) :
    (passwordModel r a c).preimage .req (reqAdmissible r) =
      (passwordModel r a c).semanticPullback (reqExtractRel r a c) (reqAdmissible r) := by
  refine Model.preimage_eq_semanticPullback (M := passwordModel r a c) .req (reqExtractRel r a c) ?hEq (reqAdmissible r)
  intro x y
  constructor
  · intro hproj
    exact (by simpa [passwordModel] using hproj : x = y).symm
  · intro hrel
    have hyx : y = x := by
      simpa [reqExtractRel] using hrel
    simp [passwordModel, hyx]

theorem checkConsistent_true_implies_allThree
    (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact)
    (h : checkConsistent r a c = true) :
    allThreeConsistent r a c := by
  unfold checkConsistent at h
  have hLe : extractedLower r a ≤ extractedUpper r c := by
    exact of_decide_eq_true h
  refine ⟨extractedLower r a, ?_, ?_, ?_⟩
  · refine ⟨extractedLower r a, rfl, ?_⟩
    constructor
    · exact Nat.le_max_left r.minLen a.minLen
    · have hUpperReq : extractedUpper r c ≤ r.maxLen := Nat.min_le_left r.maxLen c.maxLen
      exact Nat.le_trans hLe hUpperReq
  · refine ⟨extractedLower r a, rfl, ?_⟩
    exact Nat.le_max_right r.minLen a.minLen
  · refine ⟨extractedLower r a, rfl, ?_⟩
    have hUpperCode : extractedUpper r c ≤ c.maxLen := Nat.min_le_right r.maxLen c.maxLen
    exact Nat.le_trans hLe hUpperCode

theorem allThree_implies_checkConsistent_true
    (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact)
    (h : allThreeConsistent r a c) :
    checkConsistent r a c = true := by
  rcases h with ⟨n, hReq, hApi, hCode⟩
  rcases hReq with ⟨nr, hProjReq, hReqA⟩
  rcases hApi with ⟨na, hProjApi, hApiA⟩
  rcases hCode with ⟨nc, hProjCode, hCodeA⟩
  have hnr : nr = n := by
    symm
    simpa [passwordModel] using hProjReq
  have hna : na = n := by
    symm
    simpa [passwordModel] using hProjApi
  have hnc : nc = n := by
    symm
    simpa [passwordModel] using hProjCode
  have hReqBounds : r.minLen ≤ n ∧ n ≤ r.maxLen := by
    simpa [reqAdmissible, hnr] using hReqA
  have hApiMin : a.minLen ≤ n := by
    simpa [apiAdmissible, hna] using hApiA
  have hCodeMax : n ≤ c.maxLen := by
    simpa [codeAdmissible, hnc] using hCodeA
  have hLowerLeN : extractedLower r a ≤ n := by
    exact (Nat.max_le).2 ⟨hReqBounds.1, hApiMin⟩
  have hNLeUpper : n ≤ extractedUpper r c := by
    exact (Nat.le_min).2 ⟨hReqBounds.2, hCodeMax⟩
  have hFinal : extractedLower r a ≤ extractedUpper r c := Nat.le_trans hLowerLeN hNLeUpper
  unfold checkConsistent
  exact by simpa using (show decide (extractedLower r a ≤ extractedUpper r c) = true from
    by exact decide_eq_true hFinal)

theorem checkConsistent_iff_allThree
    (r : ReqArtifact) (a : ApiArtifact) (c : CodeArtifact) :
    checkConsistent r a c = true ↔ allThreeConsistent r a c := by
  constructor
  · exact checkConsistent_true_implies_allThree r a c
  · exact allThree_implies_checkConsistent_true r a c

def exConsistentReq : ReqArtifact := { minLen := 8, maxLen := 64 }
def exConsistentApi : ApiArtifact := { minLen := 10 }
def exConsistentCode : CodeArtifact := { maxLen := 128 }

example : checkConsistent exConsistentReq exConsistentApi exConsistentCode = true := by
  native_decide

def exContradictReq : ReqArtifact := { minLen := 8, maxLen := 12 }
def exContradictApi : ApiArtifact := { minLen := 16 }
def exContradictCode : CodeArtifact := { maxLen := 10 }

example : checkConsistent exContradictReq exContradictApi exContradictCode = false := by
  native_decide

end CaseStudy
end UadfU0
--- END FILE: paper/lean/UadfU0/CaseStudy/PasswordPolicy.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Definitions/Model.lean ---
namespace UadfU0

universe u v w

abbrev SpecSet (α : Type v) : Type v := α → Prop

instance {α : Type v} : Membership α (SpecSet α) where
  mem s x := s x

instance {α : Type v} : HasSubset (SpecSet α) where
  Subset s t := ∀ ⦃x : α⦄, x ∈ s → x ∈ t

def sUnion {α : Type v} (A B : SpecSet α) : SpecSet α :=
  fun x => x ∈ A ∨ x ∈ B

def sInter {α : Type v} (A B : SpecSet α) : SpecSet α :=
  fun x => x ∈ A ∧ x ∈ B

def sEmpty {α : Type v} : SpecSet α :=
  fun _ => False

infixr:65 " ∪ₛ " => sUnion
infixr:70 " ∩ₛ " => sInter
notation "∅ₛ" => sEmpty

theorem subset_refl {α : Type v} (A : SpecSet α) : A ⊆ A := by
  intro x hx
  exact hx

theorem subset_trans {α : Type v} {A B C : SpecSet α} :
    A ⊆ B → B ⊆ C → A ⊆ C := by
  intro hAB hBC x hx
  exact hBC (hAB hx)

theorem set_ext {α : Type v} {A B : SpecSet α} :
    (∀ x : α, x ∈ A ↔ x ∈ B) → A = B := by
  intro h
  funext x
  exact propext (h x)

/--
Layer in UAD/f:
- `D`: domain predicate
- `A`: admissible predicate
- `A ⊆ D`: admissible values stay inside the domain.
-/
structure Layer (β : Type w) where
  D : SpecSet β
  A : SpecSet β
  admissible_subset_domain : A ⊆ D

/--
Typed UAD/f model:
- root space `α`
- per-layer carrier type `carrier i = βᵢ`
- partial forward map `proj i : α → Option βᵢ`

`f₀ᵢ⁻¹` is represented by `preimage i`.
-/
structure Model (ι : Type u) (α : Type v) where
  carrier : ι → Type w
  layer : (i : ι) → Layer (carrier i)
  proj : (i : ι) → α → Option (carrier i)

namespace Model

variable {ι : Type u} {α : Type v}

def D (M : Model ι α) (i : ι) : SpecSet (M.carrier i) :=
  (M.layer i).D

def A (M : Model ι α) (i : ι) : SpecSet (M.carrier i) :=
  (M.layer i).A

/-- Compatibility alias. -/
def Ui (M : Model ι α) (i : ι) : SpecSet (M.carrier i) :=
  M.A i

/-- Set-level inverse image induced by typed partial projection. -/
def preimage (M : Model ι α) (i : ι) (S : SpecSet (M.carrier i)) : SpecSet α :=
  fun x => ∃ y : M.carrier i, M.proj i x = some y ∧ y ∈ S

/-- Lift one layer admissible set into root space. -/
def lifted (M : Model ι α) (i : ι) : SpecSet α :=
  M.preimage i (M.Ui i)

/-- U0 is the union of all lifted sets in root space. -/
def U0 (M : Model ι α) : SpecSet α :=
  fun x : α => ∃ i : ι, x ∈ M.lifted i

/-- Contradiction: no shared root witness across two lifted layers. -/
def Contradictory (M : Model ι α) (i j : ι) : Prop :=
  ∀ x : α, x ∈ M.lifted i → x ∈ M.lifted j → False

/-- Consistency: at least one shared root witness exists. -/
def Consistent (M : Model ι α) (i j : ι) : Prop :=
  ∃ x : α, x ∈ M.lifted i ∧ x ∈ M.lifted j

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/Definitions/Model.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Examples/ArtifactBundleExample.lean ---
import UadfU0.U0Spec.Construction

namespace UadfU0
namespace Examples

open Model

inductive LayerTag where
  | req
  | api
  | code
deriving DecidableEq

open LayerTag

structure ReqArtifact where
  lower : Nat
  upper : Nat

structure ApiArtifact where
  lower : Nat
  upper : Nat

structure CodeArtifact where
  max : Nat

structure ReqIR where
  lower : Nat
  upper : Nat

structure ApiIR where
  lower : Nat
  upper : Nat

structure CodeIR where
  max : Nat

structure ArtifactBundle where
  reqDoc : Option ReqArtifact
  apiDoc : Option ApiArtifact
  codeDoc : Option CodeArtifact

def Gamma : LayerTag → Type
  | req => ReqArtifact
  | api => ApiArtifact
  | code => CodeArtifact

def Beta : LayerTag → Type
  | req => ReqIR
  | api => ApiIR
  | code => CodeIR

def obs : (i : LayerTag) → ArtifactBundle → Option (Gamma i)
  | req, x => x.reqDoc
  | api, x => x.apiDoc
  | code, x => x.codeDoc

def extract : (i : LayerTag) → Gamma i → Option (Beta i)
  | req, g =>
      if g.lower ≤ g.upper then
        some ⟨g.lower, g.upper⟩
      else
        none
  | api, g =>
      if g.lower ≤ g.upper then
        some ⟨g.lower, g.upper⟩
      else
        none
  | code, g =>
      some ⟨g.max⟩

def projFromObsExtract (i : LayerTag) (x : ArtifactBundle) : Option (Beta i) :=
  Option.bind (obs i x) (extract i)

def layerOf : (i : LayerTag) → Layer (Beta i)
  | req =>
      {
        D := fun y : ReqIR => y.lower ≤ y.upper
        A := fun y : ReqIR => y.lower ≤ y.upper ∧ y.upper ≤ 63
        admissible_subset_domain := by
          intro y hy
          exact hy.1
      }
  | api =>
      {
        D := fun y : ApiIR => y.lower ≤ y.upper
        A := fun y : ApiIR => y.lower ≤ y.upper ∧ y.upper ≤ 63
        admissible_subset_domain := by
          intro y hy
          exact hy.1
      }
  | code =>
      {
        D := fun _ : CodeIR => True
        A := fun y : CodeIR => y.max ≤ 63
        admissible_subset_domain := by
          intro y hy
          trivial
      }

abbrev artifactBundleModel : Model LayerTag ArtifactBundle where
  carrier := Beta
  layer := layerOf
  proj := projFromObsExtract

theorem proj_bind_decomposition (i : LayerTag) (x : ArtifactBundle) :
    artifactBundleModel.proj i x = Option.bind (obs i x) (extract i) := by
  rfl

def goodBundle : ArtifactBundle where
  reqDoc := some ⟨1, 63⟩
  apiDoc := some ⟨1, 63⟩
  codeDoc := some ⟨63⟩

def brokenReqBundle : ArtifactBundle where
  reqDoc := some ⟨64, 63⟩
  apiDoc := some ⟨1, 63⟩
  codeDoc := some ⟨63⟩

example : goodBundle ∈ artifactBundleModel.U0 := by
  refine ⟨LayerTag.req, ?_⟩
  refine ⟨⟨1, 63⟩, ?_, ?_⟩
  · simp [artifactBundleModel, projFromObsExtract, obs, extract, goodBundle]
  · change 1 ≤ 63 ∧ 63 ≤ 63
    decide

example : goodBundle ∈ artifactBundleModel.UAnd := by
  intro i hi
  cases i with
  | req =>
      refine ⟨⟨1, 63⟩, ?_, ?_⟩
      · simp [artifactBundleModel, projFromObsExtract, obs, extract, goodBundle]
      · change 1 ≤ 63 ∧ 63 ≤ 63
        decide
  | api =>
      refine ⟨⟨1, 63⟩, ?_, ?_⟩
      · simp [artifactBundleModel, projFromObsExtract, obs, extract, goodBundle]
      · change 1 ≤ 63 ∧ 63 ≤ 63
        decide
  | code =>
      refine ⟨⟨63⟩, ?_, ?_⟩
      · simp [artifactBundleModel, projFromObsExtract, obs, extract, goodBundle]
      · change 63 ≤ 63
        decide

example : brokenReqBundle ∉ artifactBundleModel.lifted LayerTag.req := by
  intro hx
  rcases hx with ⟨y, hproj, _⟩
  simp [artifactBundleModel, projFromObsExtract, obs, extract, brokenReqBundle] at hproj

example : brokenReqBundle ∈ artifactBundleModel.liftedMay LayerTag.req := by
  change artifactBundleModel.proj LayerTag.req brokenReqBundle = none ∨
      ∃ y : artifactBundleModel.carrier LayerTag.req,
        artifactBundleModel.proj LayerTag.req brokenReqBundle = some y ∧ y ∈ artifactBundleModel.Ui LayerTag.req
  left
  simp [artifactBundleModel, projFromObsExtract, obs, extract, brokenReqBundle]

end Examples
end UadfU0
--- END FILE: paper/lean/UadfU0/Examples/ArtifactBundleExample.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Examples/CompositionExample.lean ---
import UadfU0.InterLayer.Composition

namespace UadfU0
namespace Examples

open Model

abbrev compositionModel : Model Bool Nat where
  carrier
  | true => Nat
  | false => Bool
  layer
  | true =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n ≥ 1
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | false =>
      {
        D := fun _ : Bool => True
        A := fun b : Bool => b = true
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  proj
  | true, x => some x
  | false, x => if x % 2 = 0 then some true else some false

def gParity : compositionModel.carrier true → Option (compositionModel.carrier false) :=
  fun n => if n % 2 = 0 then some true else some false

theorem proj_compose_holds :
    ∀ x : Nat, compositionModel.proj false x = Option.bind (compositionModel.proj true x) gParity := by
  intro x
  simp [compositionModel, gParity]

example (S : SpecSet (compositionModel.carrier false)) :
    compositionModel.preimage false S =
      compositionModel.preimage true (compositionModel.pullbackVia gParity S) := by
  exact Model.preimage_compose (M := compositionModel) true false gParity proj_compose_holds S

end Examples
end UadfU0
--- END FILE: paper/lean/UadfU0/Examples/CompositionExample.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Examples/ContradictoryLayers.lean ---
import UadfU0.InterLayer.Consistency

namespace UadfU0
namespace Examples

open Model

abbrev contradictoryModel : Model Bool Nat where
  carrier := fun _ => Nat
  layer
  | true =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n = 0
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | false =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n = 1
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  proj _ x := some x

theorem contradictoryModel_is_contradictory :
    contradictoryModel.Contradictory true false := by
  intro x hx0 hx1
  rcases hx0 with ⟨y0, hy0, hyEq0⟩
  rcases hx1 with ⟨y1, hy1, hyEq1⟩
  have hx_y0 : x = y0 := by
    simpa [contradictoryModel] using hy0
  have hx_y1 : x = y1 := by
    simpa [contradictoryModel] using hy1
  have hxEq0 : x = 0 := by
    calc
      x = y0 := hx_y0
      _ = 0 := hyEq0
  have hxEq1 : x = 1 := by
    calc
      x = y1 := hx_y1
      _ = 1 := hyEq1
  have h01 : (0 : Nat) = 1 := by
    calc
      0 = x := hxEq0.symm
      _ = 1 := hxEq1
  exact Nat.zero_ne_one h01

example : ¬ contradictoryModel.Consistent true false :=
  (Model.contradictory_iff_not_consistent (M := contradictoryModel) true false).1
    contradictoryModel_is_contradictory

end Examples
end UadfU0
--- END FILE: paper/lean/UadfU0/Examples/ContradictoryLayers.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Examples/TransferChainExample.lean ---
import UadfU0.InterLayer.Transfer

namespace UadfU0
namespace Examples

open Model

inductive ChainIx where
  | req
  | api
  | code
deriving DecidableEq, Repr

abbrev transferChainModel : Model ChainIx Nat where
  carrier := fun _ => Nat
  layer
  | .req =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n >= 1
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | .api =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n >= 3
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | .code =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n >= 5
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  proj _ x := some x

def eqRel (x y : Nat) : Prop := x = y

theorem transfer_code_to_api :
    transferChainModel.lifted .code ⊆ transferChainModel.lifted .api := by
  refine Model.lifted_transfer (M := transferChainModel) .api .code eqRel ?hproj ?hA
  · intro x yj hproj
    exact ⟨yj, by simpa [transferChainModel] using hproj, rfl⟩
  · intro yi yj hRel hyjA
    have hyi_eq_yj : yi = yj := by
      simpa [eqRel] using hRel
    have hyi_ge_five : yi >= 5 := by
      simpa [hyi_eq_yj] using hyjA
    exact Nat.le_trans (by decide : 3 <= 5) hyi_ge_five

theorem transfer_api_to_req :
    transferChainModel.lifted .api ⊆ transferChainModel.lifted .req := by
  refine Model.lifted_transfer (M := transferChainModel) .req .api eqRel ?hproj ?hA
  · intro x yj hproj
    exact ⟨yj, by simpa [transferChainModel] using hproj, rfl⟩
  · intro yi yj hRel hyjA
    have hyi_eq_yj : yi = yj := by
      simpa [eqRel] using hRel
    have hyi_ge_three : yi >= 3 := by
      simpa [hyi_eq_yj] using hyjA
    exact Nat.le_trans (by decide : 1 <= 3) hyi_ge_three

example :
    transferChainModel.lifted .code ⊆ transferChainModel.lifted .req := by
  intro x hxCode
  exact transfer_api_to_req (transfer_code_to_api hxCode)

end Examples
end UadfU0
--- END FILE: paper/lean/UadfU0/Examples/TransferChainExample.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Examples/TransferExample.lean ---
import UadfU0.InterLayer.Transfer

namespace UadfU0
namespace Examples

open Model

abbrev transferModel : Model Bool Nat where
  carrier := fun _ => Nat
  layer
  | true =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n ≥ 1
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | false =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n ≥ 2
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  proj _ x := some x

theorem transfer_false_to_true :
    transferModel.lifted false ⊆ transferModel.lifted true := by
  refine Model.lifted_transfer (M := transferModel) true false (fun yi yj => yi = yj) ?hproj ?hA
  · intro x yj hproj
    exact ⟨yj, by simpa [transferModel] using hproj, rfl⟩
  · intro yi yj hEq hyjA
    have hyj_ge_two : yj ≥ 2 := hyjA
    have hyi_ge_two : yi ≥ 2 := by simpa [hEq] using hyj_ge_two
    exact Nat.le_trans (by decide : 1 ≤ 2) hyi_ge_two

example :
    transferModel.Consistent false false → transferModel.Consistent true false := by
  intro hCons
  exact Model.consistent_transport_left (M := transferModel) true false false transfer_false_to_true hCons

end Examples
end UadfU0
--- END FILE: paper/lean/UadfU0/Examples/TransferExample.lean ---

--- BEGIN FILE: paper/lean/UadfU0/Examples/TwoLayer.lean ---
import UadfU0.InterLayer.Consistency

namespace UadfU0
namespace Examples

open Model

abbrev twoLayerTypedModel : Model Bool Nat where
  carrier
  | true => Nat
  | false => Bool
  layer
  | true =>
      {
        D := fun _ : Nat => True
        A := fun n : Nat => n ≥ 1
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  | false =>
      {
        D := fun _ : Bool => True
        A := fun b : Bool => b = true
        admissible_subset_domain := by
          intro x hx
          trivial
      }
  proj
  | true, x => some x
  | false, x => if x % 2 = 0 then some true else some false

example : 2 ∈ twoLayerTypedModel.U0 := by
  refine ⟨true, ?_⟩
  refine ⟨2, rfl, ?_⟩
  change 2 ≥ 1
  decide

example : 2 ∈ twoLayerTypedModel.UAnd := by
  intro i hi
  cases i with
  | false =>
      refine ⟨true, ?_, rfl⟩
      simp [twoLayerTypedModel]
  | true =>
      refine ⟨2, rfl, ?_⟩
      change 2 ≥ 1
      decide

example : 0 ∈ twoLayerTypedModel.U0 := by
  refine ⟨false, ?_⟩
  refine ⟨true, ?_, rfl⟩
  simp [twoLayerTypedModel]

example : twoLayerTypedModel.Consistent true false := by
  refine ⟨2, ?_, ?_⟩
  · refine ⟨2, rfl, ?_⟩
    change 2 ≥ 1
    decide
  · refine ⟨true, ?_, rfl⟩
    simp [twoLayerTypedModel]

example : ¬ twoLayerTypedModel.Contradictory true false := by
  intro hContra
  have hCons : twoLayerTypedModel.Consistent true false := by
    refine ⟨2, ?_, ?_⟩
    · refine ⟨2, rfl, ?_⟩
      change 2 ≥ 1
      decide
    · refine ⟨true, ?_, rfl⟩
      simp [twoLayerTypedModel]
  exact (Model.contradictory_iff_not_consistent (M := twoLayerTypedModel) true false).1 hContra hCons

example : twoLayerTypedModel.UAnd ⊆ twoLayerTypedModel.U0 := by
  have hsub :
      twoLayerTypedModel.UAndOn (fun _ : Bool => True) ⊆
        twoLayerTypedModel.U0On (fun _ : Bool => True) :=
    Model.UAndOn_subset_U0On (M := twoLayerTypedModel) (active := fun _ : Bool => True) ⟨true, trivial⟩
  intro x hx
  have hxAndOn : x ∈ twoLayerTypedModel.UAndOn (fun _ : Bool => True) := by
    simpa [Model.UAnd_eq_UAndOn_all (M := twoLayerTypedModel)] using hx
  have hxU0On : x ∈ twoLayerTypedModel.U0On (fun _ : Bool => True) := hsub hxAndOn
  simpa [Model.U0_eq_U0On_all (M := twoLayerTypedModel)] using hxU0On

end Examples
end UadfU0
--- END FILE: paper/lean/UadfU0/Examples/TwoLayer.lean ---

--- BEGIN FILE: paper/lean/UadfU0/InterLayer/Adequacy.lean ---
import UadfU0.U0Spec.Construction

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/-- Semantic pullback induced by an explicit extraction relation. -/
def semanticPullback
    {i : ι}
    (E : α → M.carrier i → Prop)
    (S : SpecSet (M.carrier i)) :
    SpecSet α :=
  fun x => ∃ y : M.carrier i, E x y ∧ y ∈ S

/-- May-style semantic pullback aligned with `preimageMay`. -/
def semanticPullbackMay
    {i : ι}
    (E : α → M.carrier i → Prop)
    (S : SpecSet (M.carrier i)) :
    SpecSet α :=
  fun x => M.proj i x = none ∨ ∃ y : M.carrier i, E x y ∧ y ∈ S

/--
Soundness-only variant:
if every projected witness satisfies `E`, preimage is included in semantic pullback.
-/
theorem preimage_subset_semanticPullback_of_sound
    (i : ι)
    (E : α → M.carrier i → Prop)
    (hSound : ∀ x : α, ∀ y : M.carrier i, M.proj i x = some y → E x y)
    (S : SpecSet (M.carrier i)) :
    M.preimage i S ⊆ M.semanticPullback E S := by
  intro x hx
  rcases hx with ⟨y, hproj, hyS⟩
  exact ⟨y, hSound x y hproj, hyS⟩

/--
Completeness-only variant:
if every `E`-witness is realized by projection, semantic pullback is included in preimage.
-/
theorem semanticPullback_subset_preimage_of_complete
    (i : ι)
    (E : α → M.carrier i → Prop)
    (hComplete : ∀ x : α, ∀ y : M.carrier i, E x y → M.proj i x = some y)
    (S : SpecSet (M.carrier i)) :
    M.semanticPullback E S ⊆ M.preimage i S := by
  intro x hx
  rcases hx with ⟨y, hExy, hyS⟩
  exact ⟨y, hComplete x y hExy, hyS⟩

/--
Adequacy transfer theorem.

If partial projection and extraction relation are pointwise equivalent,
the induced inverse image equals semantic pullback.
-/
theorem preimage_eq_semanticPullback
    (i : ι)
    (E : α → M.carrier i → Prop)
    (hEq : ∀ x : α, ∀ y : M.carrier i, M.proj i x = some y ↔ E x y)
    (S : SpecSet (M.carrier i)) :
    M.preimage i S = M.semanticPullback E S := by
  apply set_ext
  intro x
  constructor
  · intro hx
    exact M.preimage_subset_semanticPullback_of_sound i E (fun x y h => (hEq x y).1 h) S hx
  · intro hx
    exact M.semanticPullback_subset_preimage_of_complete i E (fun x y h => (hEq x y).2 h) S hx

/--
May-soundness:
if every projected witness satisfies `E`,
may-style preimage is included in may-style semantic pullback.
-/
theorem preimageMay_subset_semanticPullbackMay_of_sound
    (i : ι)
    (E : α → M.carrier i → Prop)
    (hSound : ∀ x : α, ∀ y : M.carrier i, M.proj i x = some y → E x y)
    (S : SpecSet (M.carrier i)) :
    M.preimageMay i S ⊆ M.semanticPullbackMay E S := by
  intro x hx
  cases hx with
  | inl hNone =>
      exact Or.inl hNone
  | inr hSome =>
      rcases hSome with ⟨y, hproj, hyS⟩
      exact Or.inr ⟨y, hSound x y hproj, hyS⟩

/--
May-completeness:
if every `E`-witness is realized by projection,
may-style semantic pullback is included in may-style preimage.
-/
theorem semanticPullbackMay_subset_preimageMay_of_complete
    (i : ι)
    (E : α → M.carrier i → Prop)
    (hComplete : ∀ x : α, ∀ y : M.carrier i, E x y → M.proj i x = some y)
    (S : SpecSet (M.carrier i)) :
    M.semanticPullbackMay E S ⊆ M.preimageMay i S := by
  intro x hx
  cases hx with
  | inl hNone =>
      exact Or.inl hNone
  | inr hSome =>
      rcases hSome with ⟨y, hExy, hyS⟩
      exact Or.inr ⟨y, hComplete x y hExy, hyS⟩

/--
May-adequacy transfer theorem:
if projection witnesses and extraction relation are pointwise equivalent,
may-style inverse image equals may-style semantic pullback.
-/
theorem preimageMay_eq_semanticPullbackMay
    (i : ι)
    (E : α → M.carrier i → Prop)
    (hEq : ∀ x : α, ∀ y : M.carrier i, M.proj i x = some y ↔ E x y)
    (S : SpecSet (M.carrier i)) :
    M.preimageMay i S = M.semanticPullbackMay E S := by
  apply set_ext
  intro x
  constructor
  · intro hx
    exact M.preimageMay_subset_semanticPullbackMay_of_sound i E
      (fun x y h => (hEq x y).1 h) S hx
  · intro hx
    exact M.semanticPullbackMay_subset_preimageMay_of_complete i E
      (fun x y h => (hEq x y).2 h) S hx

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/InterLayer/Adequacy.lean ---

--- BEGIN FILE: paper/lean/UadfU0/InterLayer/Composition.lean ---
import UadfU0.U0Spec.Construction

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/-- Pull back a target-layer set through a partial inter-layer map. -/
def pullbackVia
    {i j : ι}
    (g : M.carrier i → Option (M.carrier j))
    (S : SpecSet (M.carrier j)) :
    SpecSet (M.carrier i) :=
  fun yi => ∃ yj : M.carrier j, g yi = some yj ∧ yj ∈ S

/--
Composition law for induced inverse images.

If `proj_j = bind(proj_i, g)`, then inverse image at layer `j`
equals inverse image at layer `i` of the pullback through `g`.
-/
theorem preimage_compose
    (i j : ι)
    (g : M.carrier i → Option (M.carrier j))
    (hcomm : ∀ x : α, M.proj j x = Option.bind (M.proj i x) g)
    (S : SpecSet (M.carrier j)) :
    M.preimage j S = M.preimage i (M.pullbackVia g S) := by
  apply set_ext
  intro x
  constructor
  · intro hx
    rcases hx with ⟨yj, hprojJ, hyjS⟩
    by_cases hSome : ∃ yi : M.carrier i, M.proj i x = some yi
    · rcases hSome with ⟨yi, hprojI⟩
      refine ⟨yi, hprojI, ?_⟩
      refine ⟨yj, ?_, hyjS⟩
      have hEqBind : Option.bind (M.proj i x) g = some yj := by
        calc
          Option.bind (M.proj i x) g = M.proj j x := (hcomm x).symm
          _ = some yj := hprojJ
      simpa [hprojI] using hEqBind
    · have hNone : M.proj i x = none := by
        by_cases hpi : M.proj i x = none
        · exact hpi
        · exfalso
          cases hVal : M.proj i x with
          | none =>
              exact hpi hVal
          | some yi =>
              exact hSome ⟨yi, hVal⟩
      have hEqBind : Option.bind (M.proj i x) g = some yj := by
        calc
          Option.bind (M.proj i x) g = M.proj j x := (hcomm x).symm
          _ = some yj := hprojJ
      simp [hNone] at hEqBind
  · intro hx
    rcases hx with ⟨yi, hprojI, hpb⟩
    rcases hpb with ⟨yj, hgyi, hyjS⟩
    refine ⟨yj, ?_, hyjS⟩
    have hEqBind : Option.bind (M.proj i x) g = some yj := by
      simpa [hprojI] using hgyi
    calc
      M.proj j x = Option.bind (M.proj i x) g := hcomm x
      _ = some yj := hEqBind

/-- Lifted-set inclusion derived from an inter-layer composition law. -/
theorem lifted_subset_of_compose
    (i j : ι)
    (g : M.carrier i → Option (M.carrier j))
    (hcomm : ∀ x : α, M.proj j x = Option.bind (M.proj i x) g)
    (hA : M.pullbackVia g (M.Ui j) ⊆ M.Ui i) :
    M.lifted j ⊆ M.lifted i := by
  intro x hxj
  have hEq := M.preimage_compose i j g hcomm (M.Ui j)
  have hxj' : x ∈ M.preimage i (M.pullbackVia g (M.Ui j)) := by
    exact (hEq ▸ hxj)
  exact M.preimage_monotone i hA hxj'

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/InterLayer/Composition.lean ---

--- BEGIN FILE: paper/lean/UadfU0/InterLayer/Consistency.lean ---
import UadfU0.U0Spec.Minimality

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/-- Contradiction is equivalent to absence of a common witness. -/
theorem contradictory_iff_not_consistent (i j : ι) :
    M.Contradictory i j ↔ ¬ M.Consistent i j := by
  constructor
  · intro hContra hCons
    rcases hCons with ⟨x, hxi, hxj⟩
    exact hContra x hxi hxj
  · intro hNot x hxi hxj
    exact hNot ⟨x, hxi, hxj⟩

/-- A consistent pair guarantees that U0 is non-empty. -/
theorem consistent_implies_U0_nonempty (i j : ι)
    (h : M.Consistent i j) :
    ∃ x : α, x ∈ M.U0 := by
  rcases h with ⟨x, hxi, _⟩
  exact ⟨x, ⟨i, hxi⟩⟩

/-- If two layers are contradictory, they cannot share a witness. -/
theorem contradictory_implies_no_shared_witness (i j : ι)
    (h : M.Contradictory i j) :
    ¬ ∃ x : α, x ∈ M.lifted i ∧ x ∈ M.lifted j := by
  intro hx
  rcases hx with ⟨x, hxi, hxj⟩
  exact h x hxi hxj

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/InterLayer/Consistency.lean ---

--- BEGIN FILE: paper/lean/UadfU0/InterLayer/Transfer.lean ---
import UadfU0.InterLayer.Consistency

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/--
Layer-transfer theorem.

If every root witness projected to layer `j` can be related to some projection in layer `i`,
and that relation preserves admissibility from `j` to `i`,
then every `j`-lifted witness is also `i`-lifted.
-/
theorem lifted_transfer
    (i j : ι)
    (R : M.carrier i → M.carrier j → Prop)
    (hproj :
      ∀ x : α, ∀ yj : M.carrier j,
        M.proj j x = some yj →
        ∃ yi : M.carrier i, M.proj i x = some yi ∧ R yi yj)
    (hA :
      ∀ yi : M.carrier i, ∀ yj : M.carrier j,
        R yi yj → yj ∈ M.Ui j → yi ∈ M.Ui i) :
    M.lifted j ⊆ M.lifted i := by
  intro x hxj
  rcases hxj with ⟨yj, hproj_j, hyjA⟩
  rcases hproj x yj hproj_j with ⟨yi, hproj_i, hR⟩
  exact ⟨yi, hproj_i, hA yi yj hR hyjA⟩

/--
Consistency transport on the left argument:
if `lifted(j) ⊆ lifted(i)`, any witness consistent with `k` through `j`
is also consistent through `i`.
-/
theorem consistent_transport_left
    (i j k : ι)
    (hsub : M.lifted j ⊆ M.lifted i) :
    M.Consistent j k → M.Consistent i k := by
  intro hCons
  rcases hCons with ⟨x, hxj, hxk⟩
  exact ⟨x, hsub hxj, hxk⟩

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/InterLayer/Transfer.lean ---

--- BEGIN FILE: paper/lean/UadfU0/RelatedWork/Galois.lean ---
import UadfU0.Definitions.Model

namespace UadfU0
namespace Model

universe u v w

/-- Universal set over predicate-based set encoding. -/
def sUniv {α : Type v} : SpecSet α :=
  fun _ => True

/-- G has a left adjoint F on predicate sets if F S ⊆ T ↔ S ⊆ G T. -/
def HasLeftAdjoint
    {α : Type v}
    {β : Type w}
    (G : SpecSet β → SpecSet α) : Prop :=
  ∃ F : SpecSet α → SpecSet β, ∀ S T, F S ⊆ T ↔ S ⊆ G T

/--
If projection is undefined at some root witness, existential preimage cannot
be a right adjoint on full powersets.
-/
theorem no_left_adjoint_of_partial
    {ι : Type u}
    {α : Type v}
    (M : Model ι α)
    (i : ι)
    (x0 : α)
    (hnone : M.proj i x0 = none) :
    ¬ HasLeftAdjoint (M.preimage i) := by
  intro hAdj
  rcases hAdj with ⟨F, hGC⟩
  let S : SpecSet α := fun x => x = x0
  let T : SpecSet (M.carrier i) := sUniv
  have hLeft : F S ⊆ T := by
    intro y hy
    trivial
  have hRight : S ⊆ M.preimage i T := (hGC S T).1 hLeft
  have hxInS : x0 ∈ S := by
    show x0 = x0
    rfl
  have hxInPre : x0 ∈ M.preimage i T := hRight hxInS
  rcases hxInPre with ⟨y, hproj, _hyInT⟩
  have hproj' : M.proj i x0 = some y := hproj
  simp [hnone] at hproj'

/-- Concrete one-layer model with an undefined projection point. -/
abbrev partialProjectionModel : Model Unit Nat where
  carrier := fun _ => Nat
  layer _ :=
    {
      D := fun _ : Nat => True
      A := fun _ : Nat => True
      admissible_subset_domain := by
        intro x hx
        trivial
    }
  proj _ x := if x = 0 then none else some x

example : ¬ HasLeftAdjoint (partialProjectionModel.preimage ()) := by
  refine no_left_adjoint_of_partial (M := partialProjectionModel) () 0 ?hnone
  simp [partialProjectionModel]

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/RelatedWork/Galois.lean ---

--- BEGIN FILE: paper/lean/UadfU0/U0Spec/Construction.lean ---
import UadfU0.Definitions.Model

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/-- Expand membership of an induced inverse image. -/
theorem mem_preimage_iff (i : ι) (S : SpecSet (M.carrier i)) (x : α) :
    x ∈ M.preimage i S ↔ ∃ y : M.carrier i, M.proj i x = some y ∧ y ∈ S := Iff.rfl

/-- Inverse images induced by typed partial projection are monotone. -/
theorem preimage_monotone (i : ι) {S T : SpecSet (M.carrier i)}
    (hST : S ⊆ T) :
    M.preimage i S ⊆ M.preimage i T := by
  intro x hx
  rcases hx with ⟨y, hproj, hyS⟩
  exact ⟨y, hproj, hST hyS⟩

/-- Inverse image preserves binary unions. -/
theorem preimage_union (i : ι) (S T : SpecSet (M.carrier i)) :
    M.preimage i (S ∪ₛ T) = M.preimage i S ∪ₛ M.preimage i T := by
  apply set_ext
  intro x
  constructor
  · intro hx
    rcases hx with ⟨y, hproj, hyST⟩
    cases hyST with
    | inl hyS =>
        exact Or.inl ⟨y, hproj, hyS⟩
    | inr hyT =>
        exact Or.inr ⟨y, hproj, hyT⟩
  · intro hx
    cases hx with
    | inl hxS =>
        rcases hxS with ⟨y, hproj, hyS⟩
        exact ⟨y, hproj, Or.inl hyS⟩
    | inr hxT =>
        rcases hxT with ⟨y, hproj, hyT⟩
        exact ⟨y, hproj, Or.inr hyT⟩

/--
May-style inverse image:
undefined projection (`none`) is treated as "inconclusive" rather than immediate rejection.
-/
def preimageMay (i : ι) (S : SpecSet (M.carrier i)) : SpecSet α :=
  fun x => M.proj i x = none ∨ ∃ y : M.carrier i, M.proj i x = some y ∧ y ∈ S

/-- Must-style inverse image is always included in may-style inverse image. -/
theorem preimage_subset_preimageMay (i : ι) (S : SpecSet (M.carrier i)) :
    M.preimage i S ⊆ M.preimageMay i S := by
  intro x hx
  rcases hx with ⟨y, hproj, hyS⟩
  exact Or.inr ⟨y, hproj, hyS⟩

/-- May-style lifted admissible set. -/
def liftedMay (i : ι) : SpecSet α :=
  M.preimageMay i (M.Ui i)

/-- Must-style lifted admissible set is included in may-style lifted admissible set. -/
theorem lifted_subset_liftedMay (i : ι) :
    M.lifted i ⊆ M.liftedMay i := by
  exact M.preimage_subset_preimageMay i (M.Ui i)

/-- Because each layer satisfies `A ⊆ D`, lifted admissible points stay in lifted domains. -/
theorem lifted_subset_preimage_domain (i : ι) :
    M.lifted i ⊆ M.preimage i (M.D i) := by
  have hAD : M.Ui i ⊆ M.D i := (M.layer i).admissible_subset_domain
  exact M.preimage_monotone i hAD

/-- Membership in U0 is exactly membership in some lifted layer. -/
theorem mem_U0_iff (x : α) :
    x ∈ M.U0 ↔ ∃ i : ι, x ∈ M.lifted i := Iff.rfl

/-- Root specification induced by a selected subset of layers. -/
def U0On (active : ι → Prop) : SpecSet α :=
  fun x : α => ∃ i : ι, active i ∧ x ∈ M.lifted i

/-- Meet-style integrated specification: all active layers must hold simultaneously. -/
def UAndOn (active : ι → Prop) : SpecSet α :=
  fun x : α => ∀ i : ι, active i → x ∈ M.lifted i

/-- Meet-style integration under may semantics for undefined projections. -/
def UAndMayOn (active : ι → Prop) : SpecSet α :=
  fun x : α => ∀ i : ι, active i → x ∈ M.liftedMay i

/-- Global meet-style integrated specification over all layers. -/
def UAnd : SpecSet α :=
  M.UAndOn (fun _ : ι => True)

/-- If the active layer set grows, the induced root specification grows. -/
theorem U0On_monotone {J K : ι → Prop}
    (hJK : ∀ i : ι, J i → K i) :
    M.U0On J ⊆ M.U0On K := by
  intro x hx
  rcases hx with ⟨i, hJi, hix⟩
  exact ⟨i, hJK i hJi, hix⟩

/-- `U0` is the special case where all layers are active. -/
theorem U0_eq_U0On_all :
    M.U0 = M.U0On (fun _ : ι => True) := by
  apply set_ext
  intro x
  constructor
  · intro hx
    rcases hx with ⟨i, hix⟩
    exact ⟨i, trivial, hix⟩
  · intro hx
    rcases hx with ⟨i, _, hix⟩
    exact ⟨i, hix⟩

/-- `UAnd` is the special case where all layers are active. -/
theorem UAnd_eq_UAndOn_all :
    M.UAnd = M.UAndOn (fun _ : ι => True) := by
  rfl

/--
If the active-layer predicate grows, meet-style integration becomes stricter.
Equivalently, `UAndOn` is antitone in the active predicate.
-/
theorem UAndOn_antitone {J K : ι → Prop}
    (hJK : ∀ i : ι, J i → K i) :
    M.UAndOn K ⊆ M.UAndOn J := by
  intro x hx i hi
  exact hx i (hJK i hi)

/-- Must-style meet is included in may-style meet for the same active set. -/
theorem UAndOn_subset_UAndMayOn
    {active : ι → Prop} :
    M.UAndOn active ⊆ M.UAndMayOn active := by
  intro x hx i hi
  exact M.lifted_subset_liftedMay i (hx i hi)

/--
If may-style meet is empty, then must-style meet is also empty
(safe contradiction direction under may semantics).
-/
theorem UAndMayOn_empty_implies_UAndOn_empty
    {active : ι → Prop}
    (hEmptyMay : ∀ x : α, x ∈ M.UAndMayOn active → False) :
    ∀ x : α, x ∈ M.UAndOn active → False := by
  intro x hxMust
  exact hEmptyMay x (M.UAndOn_subset_UAndMayOn hxMust)

/--
With no active layers, meet-style integration is vacuously true for every root point.
-/
theorem UAndOn_empty_eq_univ :
    M.UAndOn (fun _ : ι => False) = (fun _ : α => True) := by
  apply set_ext
  intro x
  constructor
  · intro _hx
    trivial
  · intro _hx i hi
    exact False.elim hi

/--
If all active layers hold at `x` and at least one active layer exists,
then `x` is included in the corresponding join-style root spec.
-/
theorem UAndOn_subset_U0On
    {active : ι → Prop}
    (hActive : ∃ i : ι, active i) :
    M.UAndOn active ⊆ M.U0On active := by
  intro x hx
  rcases hActive with ⟨i, hi⟩
  exact ⟨i, hi, hx i hi⟩

/--
For any pair of layers, consistency is equivalent to non-emptiness of
the pair-restricted meet-style integrated specification.
-/
theorem consistent_iff_exists_UAndOn_pair (i j : ι) :
    M.Consistent i j ↔ ∃ x : α, x ∈ M.UAndOn (fun k : ι => k = i ∨ k = j) := by
  constructor
  · intro hCons
    rcases hCons with ⟨x, hxi, hxj⟩
    refine ⟨x, ?_⟩
    intro k hk
    cases hk with
    | inl hki =>
        simpa [hki] using hxi
    | inr hkj =>
        simpa [hkj] using hxj
  · intro h
    rcases h with ⟨x, hx⟩
    refine ⟨x, ?_, ?_⟩
    · exact hx i (Or.inl rfl)
    · exact hx j (Or.inr rfl)

/-- Every lifted layer contributes directly to U0. -/
theorem lifted_subset_U0 (i : ι) :
    M.lifted i ⊆ M.U0 := by
  intro x hx
  exact ⟨i, hx⟩

/-- If any lifted layer has a witness, U0 is non-empty. -/
theorem U0_nonempty_of_exists_layer_nonempty
    (h : ∃ i : ι, ∃ x : α, x ∈ M.lifted i) :
    ∃ x : α, x ∈ M.U0 := by
  rcases h with ⟨i, x, hx⟩
  exact ⟨x, ⟨i, hx⟩⟩

/-- Any U0 witness projects into some layer domain. -/
theorem U0_witness_projects_to_some_domain (x : α)
    (hx : x ∈ M.U0) :
    ∃ i : ι, ∃ y : M.carrier i, M.proj i x = some y ∧ y ∈ M.D i := by
  rcases hx with ⟨i, hxi⟩
  have hdom : x ∈ M.preimage i (M.D i) :=
    M.lifted_subset_preimage_domain i hxi
  rcases hdom with ⟨y, hproj, hydom⟩
  exact ⟨i, y, hproj, hydom⟩

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/U0Spec/Construction.lean ---

--- BEGIN FILE: paper/lean/UadfU0/U0Spec/IdealRoot.lean ---
import UadfU0.U0Spec.Construction

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/-- Root points where layer `i` is observable (projection is defined). -/
def projDom (i : ι) : SpecSet α :=
  fun x => ∃ y : M.carrier i, M.proj i x = some y

/-- Root points observable for every active layer. -/
def projDomOn (active : ι → Prop) : SpecSet α :=
  fun x => ∀ i : ι, active i → x ∈ M.projDom i

/--
`UStar` denotes an intended (possibly unknown) ideal root specification.
If every active layer gives a necessary condition for `UStar`,
then `UStar` is included in meet-style integration over active layers.
-/
theorem UStar_subset_UAndOn
    {active : ι → Prop}
    (UStar : SpecSet α)
    (hNecessary : ∀ i : ι, active i → UStar ⊆ M.lifted i) :
    UStar ⊆ M.UAndOn active := by
  intro x hx i hi
  exact hNecessary i hi hx

/--
Domain-restricted necessary-condition assumption:
for each active layer, only the observable part of `UStar` is required to
satisfy the layer-level lifted predicate.
-/
theorem UStar_inter_projDomOn_subset_UAndOn
    {active : ι → Prop}
    (UStar : SpecSet α)
    (hNecessaryOnDom :
      ∀ i : ι, active i →
        (fun x : α => x ∈ UStar ∧ x ∈ M.projDom i) ⊆ M.lifted i) :
    (fun x : α => x ∈ UStar ∧ x ∈ M.projDomOn active) ⊆ M.UAndOn active := by
  intro x hx i hi
  have hxDom : x ∈ M.projDom i := hx.2 i hi
  exact hNecessaryOnDom i hi ⟨hx.1, hxDom⟩

/--
Under a non-empty active set, the same necessary-condition assumption implies
`UStar` is also included in join-style root coverage over active layers.
-/
theorem UStar_subset_U0On_of_nonempty_active
    {active : ι → Prop}
    (UStar : SpecSet α)
    (hNecessary : ∀ i : ι, active i → UStar ⊆ M.lifted i)
    (hActive : ∃ i : ι, active i) :
    UStar ⊆ M.U0On active := by
  have hMeet : UStar ⊆ M.UAndOn active :=
    M.UStar_subset_UAndOn (active := active) UStar hNecessary
  exact subset_trans hMeet (M.UAndOn_subset_U0On (active := active) hActive)

/--
Domain-restricted variant:
if each active layer is required only on its observable subdomain, then
the observable part of `UStar` is included in active-layer join coverage.
-/
theorem UStar_inter_projDomOn_subset_U0On_of_nonempty_active
    {active : ι → Prop}
    (UStar : SpecSet α)
    (hNecessaryOnDom :
      ∀ i : ι, active i →
        (fun x : α => x ∈ UStar ∧ x ∈ M.projDom i) ⊆ M.lifted i)
    (hActive : ∃ i : ι, active i) :
    (fun x : α => x ∈ UStar ∧ x ∈ M.projDomOn active) ⊆ M.U0On active := by
  have hMeet :
      (fun x : α => x ∈ UStar ∧ x ∈ M.projDomOn active) ⊆ M.UAndOn active :=
    M.UStar_inter_projDomOn_subset_UAndOn (active := active) UStar hNecessaryOnDom
  exact subset_trans hMeet (M.UAndOn_subset_U0On (active := active) hActive)

/--
May-style necessary-condition assumption:
if each active layer gives a may-style necessary condition for `UStar`,
then `UStar` is included in may-style meet integration.
-/
theorem UStar_subset_UAndMayOn
    {active : ι → Prop}
    (UStar : SpecSet α)
    (hNecessaryMay : ∀ i : ι, active i → UStar ⊆ M.liftedMay i) :
    UStar ⊆ M.UAndMayOn active := by
  intro x hx i hi
  exact hNecessaryMay i hi hx

/--
Global special case: if each layer is a necessary condition for `UStar`,
then `UStar` is included in global meet-style integration.
-/
theorem UStar_subset_UAnd
    (UStar : SpecSet α)
    (hNecessaryAll : ∀ i : ι, UStar ⊆ M.lifted i) :
    UStar ⊆ M.UAnd := by
  intro x hx i _hi
  exact hNecessaryAll i hx

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/U0Spec/IdealRoot.lean ---

--- BEGIN FILE: paper/lean/UadfU0/U0Spec/Minimality.lean ---
import UadfU0.U0Spec.Construction

namespace UadfU0
namespace Model

universe u v w

variable {ι : Type u} {α : Type v}
variable (M : Model ι α)

/-- U0 is an upper bound of all lifted layers. -/
theorem U0_upper_bound (i : ι) :
    M.lifted i ⊆ M.U0 :=
  M.lifted_subset_U0 i

/-- If `B` is an upper bound of all lifted layers, then `U0 ⊆ B`. -/
theorem U0_below_every_upper_bound (B : SpecSet α)
    (hB : ∀ i : ι, M.lifted i ⊆ B) :
    M.U0 ⊆ B := by
  intro x hx
  rcases hx with ⟨i, hxi⟩
  exact hB i hxi

/-- Characterization of upper bounds through `U0`. -/
theorem U0_least_upper_bound_iff (B : SpecSet α) :
    (∀ i : ι, M.lifted i ⊆ B) ↔ M.U0 ⊆ B := by
  constructor
  · intro h
    exact M.U0_below_every_upper_bound B h
  · intro h i x hx
    exact h (show x ∈ M.U0 from ⟨i, hx⟩)

/-- Supremum-style statement in the subset order. -/
theorem U0_is_supremum :
    (∀ i : ι, M.lifted i ⊆ M.U0) ∧
    (∀ B : SpecSet α, (∀ i : ι, M.lifted i ⊆ B) → M.U0 ⊆ B) := by
  constructor
  · intro i
    exact M.U0_upper_bound i
  · intro B hB
    exact M.U0_below_every_upper_bound B hB

/-- Meet-style integrated spec is a lower bound for each active layer. -/
theorem UAndOn_lower_bound
    (active : ι → Prop)
    (i : ι)
    (hi : active i) :
    M.UAndOn active ⊆ M.lifted i := by
  intro x hx
  exact hx i hi

/-- Any common lower bound of active lifted layers is below the meet-style spec. -/
theorem below_UAndOn_of_lower_bounds
    (active : ι → Prop)
    (B : SpecSet α)
    (hB : ∀ i : ι, active i → B ⊆ M.lifted i) :
    B ⊆ M.UAndOn active := by
  intro x hxB i hi
  exact hB i hi hxB

/-- Greatest-lower-bound characterization for meet-style integrated spec. -/
theorem UAndOn_greatest_lower_bound_iff
    (active : ι → Prop)
    (B : SpecSet α) :
    (B ⊆ M.UAndOn active) ↔ (∀ i : ι, active i → B ⊆ M.lifted i) := by
  constructor
  · intro h i hi x hx
    exact (h hx) i hi
  · intro h
    exact M.below_UAndOn_of_lower_bounds active B h

end Model
end UadfU0
--- END FILE: paper/lean/UadfU0/U0Spec/Minimality.lean ---